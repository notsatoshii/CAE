---
phase: 09-changes-tab-right-rail-chat
plan: 08
type: execute
wave: 5
depends_on:
  - 09-07
files_modified:
  - dashboard/.planning/phases/09-changes-tab-right-rail-chat/09-VERIFICATION.md
autonomous: false
requirements:
  - VOI-01
  - CHG-01
  - CHG-02
  - CHG-03
  - CHT-01
  - CHT-02
  - CHT-03
  - CHT-04
  - CHT-05
  - CHT-06
  - MODEL-01
  - GATE-01
tags: [wave-5, verification, uat, human-checkpoint]

must_haves:
  truths:
    - "`09-VERIFICATION.md` exists and covers every REQ in the requirements list with a `[ ]` checklist item and an explicit verify command or manual step"
    - "`pnpm test` full suite exits 0 (all previously-green Phases 3-8 tests + new Phase 9 tests: voice-router, chat-suggestions, chat-cost-estimate, cae-changes-state, cae-chat-state, chat-rail provider, chat-rail component, change-row component, confirm-action-dialog component)"
    - "`pnpm tsc --noEmit` exits 0"
    - "`pnpm lint` exits 0 (including lint-no-dollar.sh)"
    - "`pnpm build` exits 0 with all new routes registered: /build/changes, /chat, /api/changes, /api/chat/send, /api/chat/state, /api/chat/history/[sessionId], /api/chat/sessions"
    - "Human UAT passes: user manually verifies the 10-item UAT checklist (documented below)"
    - "Before UAT: orchestrator surfaces any remaining VOICE.md sign-off gap from Wave 0's notice"
  artifacts:
    - path: "dashboard/.planning/phases/09-changes-tab-right-rail-chat/09-VERIFICATION.md"
      provides: "Signed-off verification checklist"
      contains: "VOICE"
  key_links: []
---

<objective>
Wave 5 — phase gate. Write 09-VERIFICATION.md covering every requirement, run the full test+lint+build suite one last time, and pause for human UAT.

Purpose: Every phase in this repo terminates with a VERIFICATION.md + a human sign-off. Phase 9's surface (chat + gate + timeline) is user-facing so the manual pass is mandatory — automated tests cover mechanics, humans cover feel.

Output:
- `09-VERIFICATION.md` with every REQ checklisted + commands + manual UAT items.
- Explicit human checkpoint asking the user to sign off or file gap-closure notes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@dashboard/.planning/phases/09-changes-tab-right-rail-chat/09-CONTEXT.md
@dashboard/.planning/phases/09-changes-tab-right-rail-chat/09-01-PLAN.md
@dashboard/.planning/phases/09-changes-tab-right-rail-chat/09-02-PLAN.md
@dashboard/.planning/phases/09-changes-tab-right-rail-chat/09-03-PLAN.md
@dashboard/.planning/phases/09-changes-tab-right-rail-chat/09-04-PLAN.md
@dashboard/.planning/phases/09-changes-tab-right-rail-chat/09-05-PLAN.md
@dashboard/.planning/phases/09-changes-tab-right-rail-chat/09-06-PLAN.md
@dashboard/.planning/phases/09-changes-tab-right-rail-chat/09-07-PLAN.md
@dashboard/.planning/phases/08-memory-global-top-bar-icon-page-graphify/08-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 09-VERIFICATION.md (full checklist + commands)</name>
  <files>dashboard/.planning/phases/09-changes-tab-right-rail-chat/09-VERIFICATION.md</files>
  <read_first>
    - dashboard/.planning/phases/08-memory-global-top-bar-icon-page-graphify/08-VERIFICATION.md (template + tone)
    - All Phase 9 PLAN files for the complete list of `truths` / `artifacts`
  </read_first>
  <action>
    Create `.planning/phases/09-changes-tab-right-rail-chat/09-VERIFICATION.md` following the Phase 8 structure:

    ```markdown
    # Phase 9 Verification

    **Phase:** 09-changes-tab-right-rail-chat
    **Status:** [ ] Pending UAT / [ ] Signed off
    **Signed:**     (Eric)
    **Date:**

    ---

    ## 1. Automated gate

    From `dashboard/`:

    - [ ] `pnpm install --frozen-lockfile` clean
    - [ ] `pnpm tsc --noEmit` exits 0
    - [ ] `pnpm test` exits 0. Expect at least these test files pass:
      - `lib/voice-router.test.ts`
      - `lib/chat-suggestions.test.ts`
      - `lib/chat-cost-estimate.test.ts`
      - `lib/cae-changes-state.test.ts`
      - `lib/cae-chat-state.test.ts`
      - `lib/providers/chat-rail.test.tsx`
      - `components/chat/chat-rail.test.tsx`
      - `components/chat/confirm-action-dialog.test.tsx`
      - `components/changes/change-row.test.tsx`
      - (plus every prior-phase test file)
    - [ ] `pnpm lint` exits 0 (lint-no-dollar.sh sees no `$` in new files)
    - [ ] `pnpm build` exits 0 and registers these new routes in the build output:
      - `/build/changes` (replaces Phase 8 stub)
      - `/chat`
      - `/api/changes`
      - `/api/chat/send`
      - `/api/chat/state`
      - `/api/chat/history/[sessionId]`
      - `/api/chat/sessions`

    ## 2. File presence (per locked decisions)

    - [ ] `docs/VOICE.md` exists, ≤200 lines (D-04)
    - [ ] All 9 `docs/voices/{agent}.md` exist, each ≤40 lines (D-04)
    - [ ] `lib/voice-router.ts` exports `pickPersona`, `MODEL_BY_AGENT`, `modelForAgent` (D-05, D-06)
    - [ ] `lib/chat-suggestions.ts` covers all 8 routes listed in D-11
    - [ ] `lib/chat-cost-estimate.ts` gates at >= 1000 tokens (D-07, GATE-01)
    - [ ] `lib/cae-changes-state.ts` produces 30-day dedupe'd ChangeEvent[] (D-01, D-02)
    - [ ] `lib/cae-chat-state.ts` stores jsonl at `${CAE_ROOT}/.cae/chat/<uuid>.jsonl` (D-08)
    - [ ] `components/chat/chat-rail.tsx` 48→300px on click (CHT-01, D-10)
    - [ ] `components/chat/confirm-action-dialog.tsx` gates token-spending actions (CHT-06)
    - [ ] `app/chat/page.tsx` exists and renders a 50/50 split (CHT-04, D-16)

    ## 3. Requirement coverage

    | REQ | Check |
    |-----|-------|
    | VOI-01  | `test -f dashboard/docs/VOICE.md && for a in nexus forge sentinel scout scribe phantom aegis arch herald; do test -f dashboard/docs/voices/$a.md; done` |
    | CHG-01  | /build/changes renders a prose project-grouped timeline for the 30-day window |
    | CHG-02  | ⌘Shift+D opens all technical-detail panels; founder-mode allows per-row click |
    | CHG-03  | Aggregator joins git-log merges with forge_end events by task_id |
    | CHT-01  | Rail is 48px by default; click expands to 300px |
    | CHT-02  | Unread dot shows + clears per D-09 |
    | CHT-03  | Nine agent voices — each persona fragment is a valid system-prompt file |
    | CHT-04  | /chat full-page split renders ChatMirror + ChatPanel |
    | CHT-05  | Suggestions chips appear below input for matching route |
    | CHT-06  | Clicking Run-now on a workflow shows ConfirmActionDialog with token estimate |
    | MODEL-01 | `lib/voice-router.ts MODEL_BY_AGENT` has opus-4-7 for nexus/arch/phantom; sonnet-4-6 for the other six |
    | GATE-01  | `shouldGate({type:'delegate_new'}) === true` (returns 8000 >= 1000) |

    ## 4. Human UAT checklist (run dev server: `pnpm dev`)

    Sign in first (GitHub OAuth) and confirm `/signin` does NOT render a chat rail.

    - [ ] **/build** — chat rail is visible on the right at 48px with a live-status dot
    - [ ] Click the rail → expands to 300px; input is focusable
    - [ ] Type "hey" + Enter → see streaming response from Nexus (dry, playful voice)
    - [ ] Type "@forge what's the build status?" → response attributed to Forge; persona persists on next turn
    - [ ] Collapse with Escape → rail returns to 48px
    - [ ] Navigate to `/metrics` → rail still shows; try default suggestion "Am I overspending?"
    - [ ] **/build/changes** — prose timeline appears grouped by project; today's activity shows "this morning/afternoon/evening"
    - [ ] Click `[▾ technical]` on a row → SHA + branch + GitHub link appear (if repo has a GitHub remote)
    - [ ] Toggle ⌘Shift+D → all technical panels auto-expand; copy flips to dev-speak
    - [ ] **/build/workflows** — click Run-now on any recipe → ConfirmActionDialog shows summary + token cost (no `$` anywhere); clicking Go runs the workflow, clicking Cancel doesn't
    - [ ] Toggle ⌘Shift+D → Run-now instantly executes + shows undo toast with "Undo"
    - [ ] **/build/queue** — "New job" → fill BUILDPLAN → Send → gate dialog shows with a summary containing the first 80 chars of the plan → Go → job appears in the queue
    - [ ] **/chat** — click the top-nav chat icon → lands on 50/50 split; rail disappears here
    - [ ] On /chat, pick "Changes" in the mirror dropdown → left pane shows the Changes summary; right pane's chat still works
    - [ ] Return to /build → rail reappears

    ## 5. Known limitations

    - Ctrl+T keybinding is NOT wired (D-10). Click-toggle is primary. Phase 12 ⌘K palette will add an "Open chat" command.
    - Chat mirror page uses JSON-fallback rendering for Agents/Workflows/Queue/Metrics/Memory; only Home + Changes have prose mirrors in v1 (Phase 12 polish).
    - Only three surfaces are gated in Wave 3: workflows Run-now + queue delegation. Retry-task, reassign, and outbox-approve remain ungated (documented in 09-06-SUMMARY).
    - VOICE.md sign-off is optional; user-gated at the end of Wave 0. Any drift after live testing → gap-closure plan.

    ## 6. Sign-off

    I have executed sections 1-4 and the output matches expectations.

    - [ ] Signed (Eric)
    - Date:
    - Notes:
    ```

    **Commit:** `docs(09-08): Phase 9 verification checklist`.
  </action>
  <verify>
    <automated>test -f /home/cae/ctrl-alt-elite/dashboard/.planning/phases/09-changes-tab-right-rail-chat/09-VERIFICATION.md && grep -q "VOI-01" /home/cae/ctrl-alt-elite/dashboard/.planning/phases/09-changes-tab-right-rail-chat/09-VERIFICATION.md && grep -q "GATE-01" /home/cae/ctrl-alt-elite/dashboard/.planning/phases/09-changes-tab-right-rail-chat/09-VERIFICATION.md && grep -q "MODEL-01" /home/cae/ctrl-alt-elite/dashboard/.planning/phases/09-changes-tab-right-rail-chat/09-VERIFICATION.md && grep -q "Signed" /home/cae/ctrl-alt-elite/dashboard/.planning/phases/09-changes-tab-right-rail-chat/09-VERIFICATION.md</automated>
  </verify>
  <done>
    Verification doc exists with sections 1-6 and covers every Phase 9 REQ.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT signs off on Phase 9</name>
  <what-built>
    The entire Phase 9 surface: Changes prose timeline, persistent chat rail, 9 agent voices, /chat split-screen, explain-before-doing gate on token-spending actions.
  </what-built>
  <how-to-verify>
    1. Run `cd /home/cae/ctrl-alt-elite/dashboard && pnpm dev` and sign in via GitHub OAuth.
    2. Open `09-VERIFICATION.md`. Execute sections 1 (automated gate), 2 (file presence), and 4 (human UAT checklist).
    3. For each checkbox, verify the behavior matches and tick it.
    4. If any behavior is off:
       - Record what's off under section 6 "Notes".
       - Open a gap-closure plan via `/gsd-plan-phase 09 --gaps` rather than editing in place.
    5. If all pass: sign section 6 with your name + date, commit the VERIFICATION.md.
  </how-to-verify>
  <files>dashboard/.planning/phases/09-changes-tab-right-rail-chat/09-VERIFICATION.md</files>
  <action>
    Orchestrator: pause after plan 09-07 completes. Present `.planning/phases/09-changes-tab-right-rail-chat/09-VERIFICATION.md` to the user and ask them to step through the 10-item UAT checklist while a dev server runs. Await the resume signal before executing any further work.
  </action>
  <verify>
    <automated>test -f /home/cae/ctrl-alt-elite/dashboard/.planning/phases/09-changes-tab-right-rail-chat/09-VERIFICATION.md && grep -q "Signed" /home/cae/ctrl-alt-elite/dashboard/.planning/phases/09-changes-tab-right-rail-chat/09-VERIFICATION.md</automated>
  </verify>
  <done>
    VERIFICATION.md has a signed section 6 (name + date) OR a gap plan is filed with a clear description of what must be fixed.
  </done>
  <resume-signal>
    Type "approved" OR paste the signed VERIFICATION.md snippet. If fixes needed, type "gap" + short description.
  </resume-signal>
</task>

</tasks>

<verification>
Phase-gate criteria:
- Automated: `pnpm test && pnpm tsc --noEmit && pnpm lint && pnpm build` all exit 0.
- Manual: UAT checklist fully ticked + signed in VERIFICATION.md.
</verification>

<success_criteria>
- 09-VERIFICATION.md committed.
- Human UAT signed.
- Any gaps filed as 09-gap-*-PLAN.md entries (not addressed here).
</success_criteria>

<output>
After sign-off, create `.planning/phases/09-changes-tab-right-rail-chat/09-08-SUMMARY.md` with the final status, any open gaps filed as separate plans, and a brief "Phase 9 done" lede for ROADMAP.md updating.
</output>
