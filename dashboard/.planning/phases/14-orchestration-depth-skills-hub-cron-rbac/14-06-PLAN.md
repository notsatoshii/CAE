---
phase: 14-orchestration-depth-skills-hub-cron-rbac
plan: 06
type: execute
wave: 5
depends_on: [14-01, 14-02, 14-03, 14-04, 14-05]
autonomous: false
requirements:
  - REQ-P14-01
  - REQ-P14-02
  - REQ-P14-03
  - REQ-P14-04
  - REQ-P14-05
  - REQ-P14-06
  - REQ-P14-07
  - REQ-P14-08
  - REQ-P14-09
  - REQ-P14-10
  - REQ-P14-11
  - REQ-P14-12
tags: [integration, verification, e2e, uat, final-polish]
files_modified:
  - tests/integration/phase14-skills.test.ts
  - tests/integration/phase14-schedule.test.ts
  - tests/integration/phase14-rbac.test.ts
  - tests/integration/phase14-security.test.ts
  - .planning/phases/14-orchestration-depth-skills-hub-cron-rbac/14-VERIFICATION.md
  - docs/ENV.md
  - README.md
  - components/shell/build-rail.tsx

must_haves:
  truths:
    - "All 12 REQ-P14-* requirements have integration-level proof (RTL or playwright if available)"
    - "14-VERIFICATION.md exists with manual UAT checklist for Eric"
    - "Full `pnpm test` suite green; `pnpm lint` clean; `pnpm build` clean"
    - "BuildRail final order locked: Home, Agents, Workflows, Queue, Skills, Schedules, Security, Changes (8 tabs)"
    - "docs/ENV.md updated with every Phase 14 env var + setup step"
    - "README.md has Phase 14 quickstart section (install gitleaks, create Google OAuth app, set env, install scheduler cron, install audit hook)"
    - "All existing phase tests (Phase 1-13) still green — no regressions"
  artifacts:
    - path: ".planning/phases/14-orchestration-depth-skills-hub-cron-rbac/14-VERIFICATION.md"
      provides: "Manual UAT checklist + automated results summary"
      contains: "## Manual UAT"
    - path: "tests/integration/phase14-skills.test.ts"
      provides: "End-to-end: browse catalog → install → detail → trust score"
    - path: "tests/integration/phase14-schedule.test.ts"
      provides: "NL parse → save → watcher dispatches fixture task"
    - path: "tests/integration/phase14-rbac.test.ts"
      provides: "Sign-in → role resolution → middleware gates"
    - path: "tests/integration/phase14-security.test.ts"
      provides: "Install → scan → trust score + override → audit log"
  key_links:
    - from: "14-VERIFICATION.md"
      to: "Eric's manual UAT sign-off"
      via: "checkpoint:human-verify in this plan"
      pattern: "checkpoint"

---
<objective>
Final Phase 14 wave. Four integration test suites cover cross-plan flows; 14-VERIFICATION.md captures automated proof + manual UAT checklist; README and docs/ENV.md finalized; BuildRail tab order locked; Eric's manual sign-off gate.

Purpose: Prove every plan's promises work together, not just in isolation. Ship Phase 14 with the same autonomous-gate pattern Phase 9 set (auto-approve if tsc/test/lint/build all green AND Eric signs in once with Google AND admin can install+trust-override a skill).
Output: 4 integration test files, VERIFICATION.md, README updates, ENV docs complete, BuildRail final order, Eric's checkpoint sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-orchestration-depth-skills-hub-cron-rbac/14-RESEARCH.md
@.planning/phases/14-orchestration-depth-skills-hub-cron-rbac/14-01-SUMMARY.md
@.planning/phases/14-orchestration-depth-skills-hub-cron-rbac/14-02-SUMMARY.md
@.planning/phases/14-orchestration-depth-skills-hub-cron-rbac/14-03-SUMMARY.md
@.planning/phases/14-orchestration-depth-skills-hub-cron-rbac/14-04-SUMMARY.md
@.planning/phases/14-orchestration-depth-skills-hub-cron-rbac/14-05-SUMMARY.md
@dashboard/components/shell/build-rail.tsx
@dashboard/docs/ENV.md
@dashboard/README.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 4 integration test suites covering all 12 REQ-P14-* end-to-end</name>
  <files>
    tests/integration/phase14-skills.test.ts,
    tests/integration/phase14-schedule.test.ts,
    tests/integration/phase14-rbac.test.ts,
    tests/integration/phase14-security.test.ts,
    tests/integration/helpers/fake-session.ts
  </files>
  <behavior>
    - phase14-skills.test.ts covers REQ-P14-01/02/03:
      * mount /build/skills with mocked catalog API → Catalog tab renders with 3 skills deduped from fixtures.
      * click Install on skill-card → SSE route mocked to emit line+done events → toast + catalog re-fetched.
      * click skill card → SkillDetailDrawer opens with SKILL.md content rendered + trust badge placeholder (now real from Plan 14-05).
    - phase14-schedule.test.ts covers REQ-P14-04/05:
      * type "every morning at 9am" into NlInput → CronPreview shows "At 09:00 AM" + next run.
      * Save → POST /api/schedule → scheduled_tasks.json has entry.
      * Separately: bash tests/test-scheduler-watcher.sh (from Plan 14-03) re-run as part of integration sanity.
    - phase14-rbac.test.ts covers REQ-P14-06/07/08/09:
      * Sign-in page renders GitHub + Google buttons.
      * With ADMIN_EMAILS env stub → jwt callback → session.user.role="admin".
      * With viewer role → POST /api/queue/delegate returns 403.
      * With admin role → /build/admin/roles renders email lists.
      * RoleGate with viewer currentRole hides operator-level button.
    - phase14-security.test.ts covers REQ-P14-10/11/12:
      * Skills install completes → post-install scanSkill fire-and-forget → eventually .cae/metrics/skill-scans.jsonl has a line. Test simulates with mocked gitleaks output.
      * /build/security/skills renders trust grid with scores; admin override button visible for admin, hidden for operator.
      * /build/security/audit renders audit table from fixture JSONL; tool filter shows only Bash entries when filter=Bash.
      * Audit hook matcher test: tests/test-audit-hook-matcher.sh re-run (non-Bash tools excluded).
  </behavior>
  <action>
    1. Create tests/integration/helpers/fake-session.ts:
       ```typescript
       import { vi } from "vitest";
       export function fakeSession(role: "viewer" | "operator" | "admin", email = "test@example.com") {
         vi.doMock("@/auth", () => ({
           auth: async () => ({ user: { email, role } }),
           handlers: { GET: vi.fn(), POST: vi.fn() },
           signIn: vi.fn(),
           signOut: vi.fn(),
         }));
       }
       export function clearSessionMock() { vi.doUnmock("@/auth"); }
       ```

    2. tests/integration/phase14-skills.test.ts:
       - describe("REQ-P14-01 Skills page tabs") — mount /build/skills/skills-client with 3 fixture skills → assert Catalog/Installed tabs render.
       - describe("REQ-P14-02 Install flow") — mock fetch for /api/skills/install returning mock SSE stream → click install → assert toast + refetch.
       - describe("REQ-P14-03 Detail drawer") — mock /api/skills/[name] → click card → assert drawer renders markdown + trust badge present.

    3. tests/integration/phase14-schedule.test.ts:
       - describe("REQ-P14-04 NL parse") — mount schedule-client → type in NlInput with fake timers → advance 300ms → assert CronPreview shows "At 09:00 AM".
       - describe("REQ-P14-05 Watcher dispatch") — shell out `bash tests/test-scheduler-watcher.sh` inside a vitest `it` using `execSync` with `timeout:10000` → assert exit 0 and "scheduler watcher OK" in stdout.

    4. tests/integration/phase14-rbac.test.ts:
       - describe("REQ-P14-06 Two providers") — /signin snapshot asserts two provider buttons.
       - describe("REQ-P14-07 Role resolution") — env stub + invoke jwt callback directly → assert role.
       - describe("REQ-P14-08 Middleware gate") — call middleware function with fake NextRequest + fake auth → assert redirect/403.
       - describe("REQ-P14-09 Admin page") — fakeSession("admin") + render /build/admin/roles page → assert email lists.

    5. tests/integration/phase14-security.test.ts:
       - describe("REQ-P14-11 Install triggers scan") — mock install route with scan chaining → assert appendScan called.
       - describe("REQ-P14-12 Audit filtering") — mount AuditTable with fixture → filter by tool=Bash → assert row count decreased.
       - describe("REQ-P14-10 Trust override") — fakeSession("admin") + render TrustExplainer → click override → assert POST with correct body.
       - shell test: `execSync("bash tests/test-audit-hook-matcher.sh")` → exit 0.

    6. Add to package.json scripts: `"test:integration": "vitest run tests/integration"`.

    All integration tests use RTL (already installed) for component mounts. No Playwright for v0.1 (research Wave 5 note: "Playwright if already configured; otherwise RTL"). Playwright isn't in package.json so we stay RTL.
  </action>
  <verify>
    <automated>cd /home/cae/ctrl-alt-elite/dashboard && pnpm test tests/integration 2>&1 | tail -30</automated>
  </verify>
  <done>
    4 integration test files green; each covers its REQ-P14-XX requirement block; execSync-wrapped bash tests from Plans 14-03 and 14-05 reachable from vitest.
  </done>
</task>

<task type="auto">
  <name>Task 2: Lock BuildRail final order + docs/ENV.md + README.md Phase 14 section + VERIFICATION.md</name>
  <files>
    components/shell/build-rail.tsx,
    components/shell/build-rail.test.tsx,
    docs/ENV.md,
    README.md,
    .planning/phases/14-orchestration-depth-skills-hub-cron-rbac/14-VERIFICATION.md
  </files>
  <action>
    1. Lock BuildRail TABS final order (no new tabs after this plan):
       ```typescript
       const TABS = [
         { href: "/build", icon: Home, label: "Home" },
         { href: "/build/agents", icon: Users, label: "Agents" },
         { href: "/build/workflows", icon: Zap, label: "Workflows" },
         { href: "/build/queue", icon: Inbox, label: "Queue" },
         { href: "/build/skills", icon: Puzzle, label: "Skills" },
         { href: "/build/schedule", icon: Clock, label: "Schedules" },
         { href: "/build/security", icon: ShieldCheck, label: "Security" },
         { href: "/build/changes", icon: ScrollText, label: "Changes" },
       ];
       ```
       Update build-rail.test.tsx to assert exactly this 8-tab order.

    2. Finalize docs/ENV.md — add any env we missed across plans:
       - AUTH_GOOGLE_ID, AUTH_GOOGLE_SECRET (14-04)
       - AUTH_GOOGLE_HOSTED_DOMAIN (14-04, optional)
       - ADMIN_EMAILS, OPERATOR_EMAILS (14-04)
       - CAE_ROOT (explicit override; defaults to /home/cae/ctrl-alt-elite)
       - GITLEAKS_VERSION (optional override for scripts/install-gitleaks.sh)
       - ANTHROPIC_API_KEY or claude CLI OAuth (used by Plan 14-03 LLM fallback — only needed if users hit non-rule NL)

    3. README.md new section `## Phase 14 — Orchestration depth`:
       - Brief feature overview (Skills, Schedules, Security, Permissions).
       - Setup steps (runtime dependency order):
         1. `pnpm install`
         2. `bash scripts/install-gitleaks.sh`
         3. Create `.env.local` per docs/ENV.md (Google OAuth creds + email whitelists)
         4. `bash scripts/install-audit-hook.sh` (mutation audit PostToolUse)
         5. `bash scripts/install-scheduler-cron.sh` (every-minute watcher)
         6. `pnpm dev`
       - Link to 14-VERIFICATION.md for UAT walkthrough.

    4. Create 14-VERIFICATION.md:
       ```markdown
       # Phase 14 Verification

       ## Automated results
       _To be filled by execute-plan after final run_
       - tsc: ✅/❌
       - pnpm lint: ✅/❌
       - pnpm test: NNN/NNN
       - pnpm build: ✅/❌
       - bash tests/test-scheduler-watcher.sh: ✅/❌
       - bash tests/test-audit-hook-matcher.sh: ✅/❌

       ## Requirement coverage

       | REQ | Plan | Test |
       |-----|------|------|
       | REQ-P14-01 | 14-02 | pnpm test app/build/skills + tests/integration/phase14-skills |
       | REQ-P14-02 | 14-02 | pnpm test lib/cae-skills-catalog + install route |
       | REQ-P14-03 | 14-02 | SkillDetailDrawer RTL test |
       | REQ-P14-04 | 14-03 | lib/cae-schedule-parse 20 golden cases |
       | REQ-P14-05 | 14-03 | tests/test-scheduler-watcher.sh |
       | REQ-P14-06 | 14-04 | auth.test + signin page test |
       | REQ-P14-07 | 14-04 | cae-rbac resolveRole 5 tests |
       | REQ-P14-08 | 14-04 | middleware.test 5 cases |
       | REQ-P14-09 | 14-04 | /build/admin/roles page test |
       | REQ-P14-10 | 14-05 | /build/security sub-tabs |
       | REQ-P14-11 | 14-05 | lib/cae-secrets-scan + install chaining test |
       | REQ-P14-12 | 14-05 | lib/cae-audit-log + audit-table test |

       ## Manual UAT (Eric)

       Claude cannot click these; human verification required.

       ### Skills Hub
       - [ ] Visit /build/skills. Catalog tab shows at least 5 skills deduped from skills.sh + ClawHub + local (~/.claude/skills).
       - [ ] Search "cae" filters the catalog in <500ms.
       - [ ] Click a skill card. Drawer opens with SKILL.md rendered as markdown. Trust badge shows a score.
       - [ ] Click Install on a test skill (e.g. a small repo). Live log streams. On done:0, toast appears. Skill moves to Installed tab.
       - [ ] Refresh. Installed skill persists and is marked `Installed` in catalog.

       ### Schedules
       - [ ] Visit /build/schedule. Click "New schedule".
       - [ ] Type "every morning at 9am". Preview shows "At 09:00 AM" + "Next run: tomorrow 9:00am <tz>".
       - [ ] Save with a dummy buildplan path. Entry appears in My schedules.
       - [ ] Toggle enable off / on.
       - [ ] `cat scheduled_tasks.json` at CAE_ROOT shows your entry.

       ### RBAC
       - [ ] Sign out. Visit /signin. See TWO provider buttons: GitHub + Google.
       - [ ] Sign in with a Google account NOT in ADMIN_EMAILS. You are viewer. Visit /build/admin/roles → /403.
       - [ ] Add your email to ADMIN_EMAILS in .env.local. Restart. Sign in again. /build/admin/roles renders.
       - [ ] As viewer, clicking "New schedule" submit button is disabled / 403's via middleware.

       ### Security
       - [ ] Visit /build/security. Three sub-tabs present (Skill trust, Secret scan, Tool audit).
       - [ ] Skill trust: each installed skill has a score. Click a low-scoring skill → explainer shows factors.
       - [ ] As admin, click "Mark as trusted" on one skill. Score jumps to 100 with "Trusted by admin" badge.
       - [ ] Secret scan: Rescan any skill. Result appears within seconds.
       - [ ] Tool audit: filter by tool=Bash. Rows update to show only Bash entries.
       - [ ] Trigger a Bash command in any CAE project (e.g. run a Claude Code session). Return to /build/security/audit → new entry appears.

       ## Known limitations
       - Role edits require .env.local edit + restart (DB adapter deferred to v2).
       - No skill uninstall UI (v0.1 install-only).
       - No skill publishing (read-only direction).
       - Watcher has no failover (single system-cron instance).

       ## Auto-approval criteria
       Phase 14 auto-approves if:
       - [ ] `pnpm test`, `pnpm lint`, `pnpm build` all green
       - [ ] Eric has signed in with Google at least once (proves Wave 3 integration)
       - [ ] Eric has installed at least one skill from the catalog (proves Wave 1+4 chain)
       - [ ] Eric has created at least one scheduled task (proves Wave 2)

       If headless env prevents browser UAT, defer to Eric's interactive session and note in SUMMARY.
       ```

    5. Update README.md top-level feature list to mention Phase 14 capabilities (one-liner each).

    Per research §Wave 5 note: if headless env blocks browser UAT, we still green-light with tsc/test/lint/build + documented Eric interactive session (matches Phase 9 auto-approval pattern).
  </action>
  <verify>
    <automated>cd /home/cae/ctrl-alt-elite/dashboard && pnpm test components/shell/build-rail 2>&1 | tail -5 && test -f docs/ENV.md && test -f .planning/phases/14-orchestration-depth-skills-hub-cron-rbac/14-VERIFICATION.md && echo OK</automated>
  </verify>
  <done>
    BuildRail locked at 8 tabs. docs/ENV.md has every Phase 14 env var. README has Phase 14 quickstart. 14-VERIFICATION.md exists with automated + manual sections + auto-approval criteria.
  </done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression run + fill VERIFICATION automated results + decision coverage report</name>
  <files>
    .planning/phases/14-orchestration-depth-skills-hub-cron-rbac/14-VERIFICATION.md
  </files>
  <action>
    1. Run full test suite + capture counts:
       ```bash
       cd /home/cae/ctrl-alt-elite/dashboard
       pnpm tsc --noEmit 2>&1 | tail -5
       pnpm lint 2>&1 | tail -5
       pnpm test 2>&1 | tail -10
       pnpm build 2>&1 | tail -10
       bash tests/test-scheduler-watcher.sh
       bash tests/test-audit-hook-matcher.sh
       ```

    2. Fill the "Automated results" section of 14-VERIFICATION.md with pass/fail + test counts.

    3. Append "## Plans summary" section linking to each 14-NN-SUMMARY.md.

    4. Append "## Requirement coverage matrix" with each REQ-P14-XX → Plan → Test command → Pass/Fail.

    5. If any test failure: add a "## Known Failures" section with {REQ, failing test, suspected cause, mitigation}. This is NOT a pass; executor should flag it back to orchestrator. But Phase 14 can ship with minor failures if they're documented + non-blocking (Phase 9 precedent).

    6. Commit 14-VERIFICATION.md via orchestrator (this plan doesn't commit per phase rules).
  </action>
  <verify>
    <automated>cd /home/cae/ctrl-alt-elite/dashboard && pnpm test 2>&1 | tail -5 && pnpm lint 2>&1 | tail -3 && pnpm build 2>&1 | tail -3 && grep -c "REQ-P14" .planning/phases/14-orchestration-depth-skills-hub-cron-rbac/14-VERIFICATION.md</automated>
  </verify>
  <done>
    VERIFICATION.md automated section filled with real test/lint/build/regression results. All 12 REQs covered. Any failures documented with mitigation plan.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Eric UAT + auto-approval decision</name>
  <what-built>
    Phase 14 is code-complete. Five plans delivered:
    - Skills Hub at /build/skills (browse + install + detail)
    - NL scheduler at /build/schedule (parse + save + watcher dispatches)
    - Google SSO + 3-role RBAC (whitelist-based, middleware-gated)
    - Admin-only /build/admin/roles (read-only v0.1)
    - Security panel at /build/security (trust scores + secret scan + audit log)
    - BuildRail locked at 8 tabs
    - PostToolUse audit hook registered alongside Phase 8 memory-consult hook
    - System-cron watcher installed (user crontab)
  </what-built>
  <how-to-verify>
    1. Run setup (ONCE per environment):
       ```
       bash scripts/install-gitleaks.sh
       bash scripts/install-audit-hook.sh
       bash scripts/install-scheduler-cron.sh
       ```

    2. Configure .env.local per docs/ENV.md:
       - Create Google OAuth app at console.cloud.google.com → AUTH_GOOGLE_ID + AUTH_GOOGLE_SECRET
       - ADMIN_EMAILS=<your-email>
       - OPERATOR_EMAILS= (optional for solo use)

    3. `pnpm dev` and walk through 14-VERIFICATION.md "Manual UAT" checklist (4 sections, ~10 minutes).

    4. Auto-approval criteria for /gsd-verify-work (from 14-VERIFICATION.md):
       - `pnpm test` + `pnpm lint` + `pnpm build` green
       - Signed in with Google once
       - Installed one skill
       - Created one scheduled task

    5. If headless env (no browser available), check the "defer to interactive session" note and auto-approve with that caveat documented.
  </how-to-verify>
  <files>N/A — human verification, no files touched by Claude in this task</files>
  <action>Orchestrator pauses for Eric's manual UAT per how-to-verify above. Claude does not automate this task — resume on Eric's signal.</action>
  <verify>Eric responds with "approved" OR describes issues; orchestrator routes accordingly.</verify>
  <done>Eric's sign-off received OR issues documented for gap-closure follow-up.</done>
  <resume-signal>
    Type "approved" OR describe what didn't work (we'll loop back via /gsd-plan-phase --gaps).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Verification plan itself is read-only; no new trust boundaries introduced. All threats captured in Plans 14-01 through 14-05.

## STRIDE Threat Register (inherited)

Full register per-plan. This plan's role: confirm each mitigation's test is present in the automated suite and each "accept" disposition is documented in 14-VERIFICATION.md "Known Limitations".

| Threat ID | Status |
|-----------|--------|
| All T-14-0X-XX | Verified via plan-level tests + integration suite |
</threat_model>

<verification>
- All per-plan `pnpm test` invocations green.
- `pnpm test tests/integration` → 4 suites green.
- `pnpm tsc --noEmit` → no errors.
- `pnpm lint` → no errors.
- `pnpm build` → no errors; all 10+ new routes registered.
- `bash tests/test-scheduler-watcher.sh` → OK.
- `bash tests/test-audit-hook-matcher.sh` → OK.
- 14-VERIFICATION.md exists with filled automated results + manual UAT section.
- Eric's UAT checkpoint signed off (or deferred with documented reason).
</verification>

<success_criteria>
- All 12 REQ-P14-* green in verification matrix.
- Integration suite proves cross-plan flows.
- Phase 14 auto-approves per criteria OR has Eric's explicit sign-off.
- No Phase 1-13 regressions.
- Docs complete for admin setup.
</success_criteria>

<output>
After completion, create `.planning/phases/14-orchestration-depth-skills-hub-cron-rbac/14-06-SUMMARY.md` — include links to all 5 prior SUMMARYs + VERIFICATION.md.
</output>
