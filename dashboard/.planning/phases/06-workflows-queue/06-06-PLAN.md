---
phase: 06-workflows-queue
plan: 06
type: execute
wave: 5
depends_on: [06-01, 06-02, 06-03, 06-04, 06-05]
files_modified:
  - dashboard/.planning/phases/06-workflows-queue/06-VERIFICATION.md
autonomous: false
requirements:
  - wf-verification
must_haves:
  truths:
    - "Full phase build is clean (pnpm tsc + pnpm build)"
    - "Every 06-01 through 06-05 plan's must_haves are ticked via concrete commands"
    - "End-to-end round-trip works: draft NL → save → listed → run → queue card appears"
    - "Dev-mode toggle flips both Workflows form (NL↔Monaco) and Queue column names (Waiting↔Planned, etc.)"
  artifacts:
    - path: "dashboard/.planning/phases/06-workflows-queue/06-VERIFICATION.md"
      provides: "Full verification record: build output, curl responses, grep hits, screenshots, gap list"
  key_links: []
---

<objective>
Single integration pass: run the full phase through its paces, verify every must-have from 06-01 through 06-05, and commit the results to `06-VERIFICATION.md`. This plan has ONE auto task (running the automated checks) and ONE human checkpoint (visual sign-off on the two UI flows).

Purpose: Lock Phase 6 as shipped before Phase 7 starts. Identify any gaps that need a `--gaps` follow-up plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@dashboard/.planning/phases/06-workflows-queue/06-CONTEXT.md (Quality gate)
@dashboard/.planning/phases/06-workflows-queue/06-01-PLAN.md §success_criteria
@dashboard/.planning/phases/06-workflows-queue/06-02-PLAN.md §success_criteria
@dashboard/.planning/phases/06-workflows-queue/06-03-PLAN.md §success_criteria
@dashboard/.planning/phases/06-workflows-queue/06-04-PLAN.md §success_criteria
@dashboard/.planning/phases/06-workflows-queue/06-05-PLAN.md §success_criteria
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated verification sweep + write 06-VERIFICATION.md</name>
  <files>dashboard/.planning/phases/06-workflows-queue/06-VERIFICATION.md</files>
  <read_first>
    All 5 plans' success_criteria blocks (list above)
  </read_first>
  <action>
    Run EVERY check below sequentially and capture results into `.planning/phases/06-workflows-queue/06-VERIFICATION.md`. The file should have this structure:

    ```markdown
    # Phase 6 — Workflows + Queue — VERIFICATION

    **Verified:** <ISO timestamp>
    **Status:** <PASS | PARTIAL | FAIL>

    ## Build

    - `pnpm tsc --noEmit`: <exit code> (<paste any errors>)
    - `pnpm build`: <exit code> (<paste route manifest for /api/workflows*, /api/queue, /build/workflows*, /build/queue>)

    ## Plan 06-01: Domain layer

    - [ ] `npx tsx lib/cae-workflows.test.ts` exit 0
    - [ ] `npx tsx lib/cae-nl-draft.test.ts` exit 0
    - [ ] labels.ts grep: `workflowsPageHeading`, `workflowsDraftBtn`, `queueKanbanColWaiting`, `queueKanbanColShipped` all present
    - [ ] Round-trip check: `npx tsx -e "..."` prints `true`

    ## Plan 06-02: API routes

    - [ ] `npx tsx lib/cae-queue-state.test.ts` exit 0
    - [ ] `npx tsx app/api/workflows/route.test.ts` exit 0
    - [ ] `curl -b <auth-cookie> http://localhost:3002/api/workflows` returns {workflows: [...]}
    - [ ] `curl -b <auth-cookie> http://localhost:3002/api/queue` returns {columns: {...}, counts: {...}, fetchedAt: <number>}
    - [ ] grep confirms `spawn("tmux"` present in app/api/workflows/[slug]/run/route.ts
    - [ ] grep confirms `auth()` call in run route

    ## Plan 06-03: Widgets

    - [ ] `@monaco-editor/react` in package.json deps
    - [ ] `node_modules/@monaco-editor/react` exists
    - [ ] `npx tsx components/workflows/step-graph.test.tsx` exit 0
    - [ ] grep confirms `dynamic(() => import('@monaco-editor/react'), { ssr: false ...` in monaco-yaml-editor.tsx
    - [ ] grep confirms NO `react-flow`/`dagre`/`mermaid` imports anywhere under dashboard/

    ## Plan 06-04: Workflows pages

    - [ ] Route manifest contains `/build/workflows`, `/build/workflows/new`, `/build/workflows/[slug]`
    - [ ] Phase 5 stub copy "Coming in Phase 6" GONE from app/build/workflows/page.tsx (grep -L)
    - [ ] Form shows NL section with useDevMode=false, Monaco section with useDevMode=true (HTML snapshot diff via rendering test OR human check)

    ## Plan 06-05: Queue KANBAN

    - [ ] Route manifest contains `/build/queue`
    - [ ] Page source contains `QueueKanbanClient`, `NewJobModal`, `TaskDetailSheet`
    - [ ] Page source does NOT contain `queueInboxHeading`, `TableHead`, `<Table>` (Phase 2 table deleted)
    - [ ] `actions.ts` unchanged: `git diff app/build/queue/actions.ts` empty
    - [ ] 5 columns in queue-kanban-client.tsx (grep count == 5 for `queue-column-`)

    ## End-to-end round-trip (human-verify gate)

    See checkpoint task below.

    ## Gaps / blockers

    - <list each item not ticked with justification + proposed follow-up plan>
    ```

    Builder: open a dev server (`pnpm dev` on :3002) only if not already running. Use curl-with-cookie via signed-in session if available; OR note "auth-gated — verified via route manifest instead" for routes that require auth.

    Populate the `Gaps / blockers` section with any item that is NOT ticked. For each gap: reference which 06-0N plan owns it + why it wasn't satisfied. If gaps are non-empty, the overall Status is `PARTIAL` and the user should run `/gsd-plan-phase 06-workflows-queue --gaps` to schedule closure plans.

    Also append a short `## Summary` at the top (2-3 bullets: what shipped, what's deferred, total plans + tasks + commits).

    Commit the VERIFICATION.md:

    ```bash
    cd /home/cae/ctrl-alt-elite/dashboard
    git add .planning/phases/06-workflows-queue/06-VERIFICATION.md
    git commit -m "docs(06-verify): phase 6 verification record"
    ```
  </action>
  <verify>
    <automated>cd /home/cae/ctrl-alt-elite/dashboard && test -f .planning/phases/06-workflows-queue/06-VERIFICATION.md && grep -q "## Build" .planning/phases/06-workflows-queue/06-VERIFICATION.md && grep -q "## Plan 06-05" .planning/phases/06-workflows-queue/06-VERIFICATION.md && pnpm tsc --noEmit && pnpm build 2>&1 | tail -40</automated>
  </verify>
  <done>
    - 06-VERIFICATION.md exists with all 5 plan sections + Summary + Gaps
    - pnpm tsc clean, pnpm build clean
    - VERIFICATION.md committed
    - All automated checks passed OR clearly documented as gap items
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human sign-off on two UI flows</name>
  <files>N/A — checkpoint task produces no files, only a user approval signal</files>
  <what-built>
    Complete Phase 6 workflows + queue UI (already built by 06-01 through 06-05):
    - /build/workflows list with Run-now buttons
    - /build/workflows/new with NL-draft flow
    - /build/workflows/[slug] edit with Monaco when dev-mode on
    - /build/queue 5-column KANBAN with click-to-sheet + new-job modal
  </what-built>
  <action>
    HALT auto execution and prompt the user to run the two flows below. Builder does NOT automate these flows — the user performs them manually in a browser and reports back.

    Instructions for the user (paste verbatim when the checkpoint triggers):

    Run `pnpm dev` (or use an already-running dev server on :3002). Sign in with GitHub.

    **Flow 1 — Workflow creation round-trip:**
    1. Visit http://localhost:3002/build/workflows. Confirm: left rail highlights Workflows (⚡). Empty state or existing rows render. Title reads "Recipes" (founder default).
    2. Click "New recipe" → land on /build/workflows/new. Title reads "New recipe".
    3. In the natural-language textarea, type: `Every Monday, Forge runs pnpm test, Sentinel reviews, I approve, then push to main`
    4. Click "Draft it". Confirm: step graph on the right shows 4 boxes — Forge (cyan), Sentinel (cyan), Gate/Approval (amber), Push action (green). The name field appears populated (probably "every-monday-forge-runs-pnpm" per heuristic). Rename to `test-recipe`.
    5. Press Ctrl+Shift+D to toggle Dev mode. Confirm: textarea swaps to Monaco YAML editor, YAML matches what was drafted. Edit the YAML (add a space or change a task string). Ctrl+Shift+D again to flip back to founder mode — confirm Monaco unmounts and NL textarea reappears.
    6. Click "Save recipe". Confirm: toast "Recipe saved", navigated back to /build/workflows, new row visible showing `test-recipe` with "4 steps" + "just now" timestamps.
    7. Click the row's "Run now" button. Confirm: toast "Started — task wf-test-recipe-..." with a hint to check Queue. Check `.cae/inbox/` on disk — new `wf-test-recipe-*/` directory with BUILDPLAN.md + META.yaml.
    8. Click the row title → lands on /build/workflows/test-recipe. Form is pre-populated. Click "Delete recipe" → confirm dialog → toast "Recipe deleted", redirect to list, row gone.

    **Flow 2 — Queue KANBAN:**
    1. Visit http://localhost:3002/build/queue. Confirm: 5 columns labeled "Waiting", "Working on it", "Double-checking", "Stuck", "Shipped" (founder default).
    2. Press Ctrl+Shift+D → labels flip to "Planned", "Building", "Reviewing", "Blocked", "Merged". Ctrl+Shift+D again to restore.
    3. Any existing inbox tasks appear in Waiting; active tmux sessions appear in "Working on it" with a pulsing cyan dot.
    4. Click "New job" → Dialog modal opens with the existing Phase 2 delegate form. Submit a test BUILDPLAN. Modal closes, toast "Job queued", within 5s the Waiting column gets a new card.
    5. Click the new card. Confirm: right-slide TaskDetailSheet opens (Phase 4 component). URL gains `?sheet=open&task=web-...&project=...`. Sheet heading reads "Phase ?" (acceptable — queue tasks aren't phase-scoped in Phase 6). Close with Esc.

    Report any discrepancies as-is — no massaging. If both flows pass cleanly: type "approved". Otherwise: list each issue by flow + numbered step.
  </action>
  <verify>
    <automated>echo "Checkpoint task — no automated verify. User must type 'approved' or list issues to resume."</automated>
  </verify>
  <done>
    - User has typed "approved" OR has provided a list of discrepancies
    - If approved: Phase 6 integration is considered shipped
    - If discrepancies: the orchestrator routes to `/gsd-plan-phase 06-workflows-queue --gaps` with the discrepancy list captured in 06-VERIFICATION.md's "Gaps / blockers" section
  </done>
  <resume-signal>Type "approved" or describe each issue observed (per flow, numbered step).</resume-signal>
</task>

</tasks>

<verification>
1. Task 1 auto checks complete + 06-VERIFICATION.md committed
2. Task 2 human checkpoint: user typed "approved" OR list of issues captured
3. If issues captured: orchestrator routes to `/gsd-plan-phase 06-workflows-queue --gaps`
</verification>

<success_criteria>
- 06-VERIFICATION.md exists with concrete command output from every must-have
- User has signed off on both end-to-end flows OR issues are documented for gap-closure
- Phase 6 is ready to mark complete in ROADMAP.md + STATE.md (orchestrator's job, not this plan)
</success_criteria>

<output>
After completion, create `.planning/phases/06-workflows-queue/06-06-SUMMARY.md` using `$HOME/.claude/get-shit-done/templates/summary.md`. Include the user's sign-off signal (approved / issues list) verbatim in the SUMMARY.
</output>
