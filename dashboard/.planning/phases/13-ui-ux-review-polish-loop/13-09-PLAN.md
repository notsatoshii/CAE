---
phase: 13-ui-ux-review-polish-loop
plan: 09
type: execute
wave: 6
depends_on: [13-04, 13-06, 13-07, 13-08]
files_modified:
  - app/globals.css
  - tailwind.config.ts
  - components/shell/top-nav.tsx
  - components/shell/cost-ticker.tsx
  - components/shell/heartbeat-dot.tsx
  - components/shell/liveness-chip.tsx
  - components/shell/alert-banner.tsx
  - components/shell/ambient-clock.tsx
  - components/build-home/rollup-strip.tsx
  - components/build-home/active-phase-cards.tsx
  - components/build-home/needs-you-list.tsx
  - components/build-home/recent-ledger.tsx
  - components/build-home/live-ops-line.tsx
  - app/build/page.tsx
  - lib/copy/labels.ts
  - .planning/phases/13-ui-ux-review-polish-loop/audit/UI-AUDIT-visual-pillars.md
  - .planning/phases/13-ui-ux-review-polish-loop/audit/UI-AUDIT-visual-top-and-home.md
autonomous: true
requirements:
  - REQ-P13-07
  - REQ-P13-10

must_haves:
  truths:
    - "Every shipped visual pillar scores ≥ 3/4 on the top nav, alert banner, and build-home surfaces per the V2 §6 rubric"
    - "text-dim (#5a5a5c, 2.7:1 contrast) is NOT used on body copy anywhere on those surfaces — replaced with text-muted (#8a8a8c, 5.1:1) per WCAG 2.2 AA SC 1.4.3"
    - "Rollup strip uses MC-style card treatment (5 distinct elevated cards with icons + colored dots) per V2 §5 row 3 deferred adoption"
    - "All spacing is on the 8pt grid: values ∈ {4, 8, 12, 16, 24, 32, 48, 64} — no arbitrary p-[23px] on touched surfaces"
    - "Motion respects prefers-reduced-motion: reduce — LivenessChip ticks pause; screen shake only on Sentinel merge; no perpetual pulse outside strict liveness"
    - "Typography: Geist Sans + Geist Mono only; scale {13, 14, 15, 16, 20, 24, 32}px; weights 400/500/600 only; no Times New Roman leakage"
  artifacts:
    - path: ".planning/phases/13-ui-ux-review-polish-loop/audit/UI-AUDIT-visual-pillars.md"
      provides: "Full 6-pillar audit output across ALL shipped surfaces — the foundation document that plans 13-10/11 also consume"
      min_lines: 120
    - path: ".planning/phases/13-ui-ux-review-polish-loop/audit/UI-AUDIT-visual-top-and-home.md"
      provides: "Plan-13-09 specific subset of pillar audit + per-file fix list for top nav + build home + alert banner surfaces"
      min_lines: 60
    - path: "app/globals.css"
      provides: "Token updates + any component-class polish for top-nav + build-home"
      contains: "text-muted"
    - path: "components/build-home/rollup-strip.tsx"
      provides: "MC-style 5-card grid with icons + colored dots, not the single-row strip (visual treatment per V2 §5 row 3)"
      contains: "icon"
  key_links:
    - from: "audit/UI-AUDIT-visual-pillars.md"
      to: "plans 13-10 (agents/queue/changes/workflows) + 13-11 (memory/metrics/chat/plan)"
      via: "shared pillar rubric output — 13-10/11 consume per-surface rows"
      pattern: "pillars"
    - from: "tailwind.config.ts 8pt spacing scale"
      to: "component classNames (p-1/p-2/p-3/p-4/p-6/p-8/p-12/p-16)"
      via: "Tailwind scale mapping"
      pattern: "spacing"
---

<objective>
Wave 6 Part 1 — visual 6-pillar audit + polish on the THREE surfaces Eric sees most often:
- Top nav (cost ticker, heartbeat, ambient clock, liveness chip, alert banner, user menu)
- /build home (rollup strip, active phase cards, needs-you list, recent ledger, live-ops line)
- Alert banner

Also produces the FULL 6-pillar audit report (covering all shipped surfaces) that plans 13-10 and 13-11 consume for their partial-surface polish work.

Purpose: resolve Eric's "design mid at best" critique on the highest-frequency surfaces first. Numbers, liveness chips, alerts — all need to hit ≥3/4 on hierarchy/density/consistency/motion/typography/color before moving to lower-traffic surfaces.

Output: 1 global audit doc + 1 plan-specific sub-doc + polished top nav + polished build home + MC-style rollup cards + WCAG fixes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-ui-ux-review-polish-loop/13-RESEARCH-V2.md
@docs/UI-SPEC.md
@.planning/phases/13-ui-ux-review-polish-loop/shots/before/MANIFEST.tsv
@.planning/phases/13-ui-ux-review-polish-loop/shots/before/laptop-founder/build-home.png
@.planning/phases/13-ui-ux-review-polish-loop/shots/before/mobile-founder/build-home.png
@.planning/phases/13-ui-ux-review-polish-loop/shots/before/wide-founder/build-home.png
@.planning/phases/13-ui-ux-review-polish-loop/shots/before/laptop-founder/build-task-sheet.png
@.planning/phases/13-ui-ux-review-polish-loop/reference/overview.png
@app/globals.css
@tailwind.config.ts
@components/shell/top-nav.tsx
@components/shell/cost-ticker.tsx
@components/shell/heartbeat-dot.tsx
@components/shell/alert-banner.tsx
@components/shell/liveness-chip.tsx
@components/shell/ambient-clock.tsx
@components/build-home/rollup-strip.tsx
@components/build-home/active-phase-cards.tsx
@components/build-home/needs-you-list.tsx
@components/build-home/recent-ledger.tsx
@components/build-home/live-ops-line.tsx
@app/build/page.tsx

<interfaces>
<!-- V2 §6 scoring + rubric (binding). 4-point scale; ≥3 on every pillar; NO pillar scores 1. -->

### Pillars (recap)
1. **Hierarchy** — focal point, size 1.5× secondary, ≤2 weights
2. **Density** — 8pt grid spacing, list 12-14px padding / 32-40px rows
3. **Consistency** — same action = same visual; Lucide icons only; CTAs through labels.ts
4. **Motion** — prefers-reduced-motion respected; no perpetual pulse except liveness
5. **Typography** — Geist sans + mono; scale {13,14,15,16,20,24,32}; weights 400/500/600
6. **Color** — WCAG 2.2 AA: body ≥4.5:1, UI/large ≥3:1

### Pre-surfaced findings (from V2 §6 and §9)
- `--text-dim` (#5a5a5c) on bg #0a0a0a = 2.7:1 — fails WCAG for body. Used in multiple places. Find via grep.
- Rollup strip "Warnings: 0" always-amber dot — V2 pitfall #2 (verify in shot)
- Empty states look like broken states — V2 pitfall #3 (Idle right now / — tok today)
- Possible Times New Roman fallback — V2 §6 pillar 5 1-point trigger; spot-check `font-family` computed in shots
- Motion surfaces to audit for prefers-reduced-motion: LivenessChip ticking (plan 13-06), AmbientClock ticking (plan 13-07), screen shake on merge

### Vision audit prompt (V2 §7 — use Opus 4.7 via Read on PNGs, 6-8 per batch)
```
You are auditing screenshots of the CAE Dashboard against a locked design contract:
- Colors: bg #0a0a0a, text #e8e8e8, text-muted #8a8a8c (4.5:1+), text-dim #5a5a5c (2.7:1 — FAILS body), accent #00d4ff
- Typography: Geist Sans + Geist Mono only; scale {13,14,15,16,20,24,32}px; weights 400/500/600
- Density: 8pt grid values {4,8,12,16,24,32,48,64}
- Motion: prefers-reduced-motion respected; no perpetual pulse outside strict liveness
- Accent reserved: primary CTA, active nav, focus ring, liveness

For EACH image attached (labeled image-1, image-2, ...), emit JSON:
{
  "image": "image-1",
  "route": "/build",
  "viewport": "laptop-founder",
  "pillar_scores": { hierarchy: N, density: N, consistency: N, motion: N, typography: N, color: N },
  "findings": [{
    "severity": "P0|P1|P2",
    "pillar": "...",
    "element": "<specific reference>",
    "defect": "<what's wrong, pixel-level>",
    "fix": "<concrete change>",
    "wcag_sc": "1.4.3 | null"
  }],
  "strengths": ["..."]
}

Be critical. Eric has said no surface-level passes.
```

### Surfaces THIS plan audits + polishes
1. Top nav (all 3 viewports × 2 modes = 5 captures, founder-default + dev)
2. Build home (all 3 viewports × 2 modes = 5 captures)
3. Build task sheet (1 drawer state at laptop-founder)
4. Alert banner (simulated — either screenshot a dev trigger or extract from the code; rubric applies regardless)

### MC rollup card treatment (V2 §5 row 3 deferred adoption)
Convert single-row rollup strip into 5 distinct elevated cards:
```tsx
<div className="grid grid-cols-2 gap-3 md:grid-cols-3 lg:grid-cols-5">
  {slots.map(s => (
    <div key={s.key} className="relative rounded-lg border border-[color:var(--border)] bg-[color:var(--bg-elev)] p-4">
      <s.Icon className="absolute right-3 top-3 size-4 text-[color:var(--text-muted)]" aria-hidden />
      <div className="text-[32px] font-semibold tabular-nums text-[color:var(--text)]">{s.value}</div>
      <div className="mt-1 flex items-center gap-1.5">
        <span className="inline-block size-1.5 rounded-full" style={{backgroundColor: s.warning ? "var(--warning)" : "var(--text-dim)"}} aria-hidden />
        <div className="text-[13px] text-[color:var(--text-muted)]">{s.label}</div>
      </div>
    </div>
  ))}
</div>
```
Icons: Lucide (per UI-SPEC consistency pillar). `Package` for shipped, `Zap` for in-flight, `AlertTriangle` for warnings, `CircleDollarSign` for tokens, `PauseCircle` for blocked.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full 6-pillar vision audit across all shipped surfaces (produces master UI-AUDIT-visual-pillars.md)</name>
  <files>.planning/phases/13-ui-ux-review-polish-loop/audit/UI-AUDIT-visual-pillars.md</files>
  <action>
  1. Enumerate capture batches (aim 6-8 PNGs per Opus vision read — batches of 7 per V2 §7):
     - Batch A: top-nav crops + LivenessChip + AlertBanner (7 images)
     - Batch B: /build home laptop-founder + mobile + wide (3 images)
     - Batch C: /build/agents laptop-founder + dev (2 images)
     - Batch D: /build/queue + /build/changes + /build/workflows + workflows/new (4 images)
     - Batch E: /metrics laptop + mobile + wide (3 images)
     - Batch F: /memory browse + graph + why-drawer + timeline (4 images)
     - Batch G: /chat + /plan + signin (3 images)
     - Batch H: drawer states (task-sheet, agent-drawer, memory-why — 3 images)

  2. For each batch, open the PNG files via the Read tool (native vision), apply V2 §7 prompt verbatim.

  3. Concatenate JSON outputs into master `audit/UI-AUDIT-visual-pillars.md`:
     - Top-level table: `| Surface | Viewport | Mode | Hierarchy | Density | Consistency | Motion | Typography | Color |`
     - Findings section per severity: P0 / P1 / P2
     - Cross-surface patterns (e.g., "text-dim used on body across 12 surfaces" becomes a SINGLE P0 finding with 12 file pointers rather than 12 findings)
     - Strengths section: what NOT to change

  4. Flag rows applicable to THIS plan (top nav, build home, alert banner) vs plans 13-10 (agents, queue, changes, workflows) vs 13-11 (memory, metrics, chat, plan).

  5. If any pillar scores 1 on any shipped surface, document explicitly — that's the phase-gate blocker per V2 §6.

  Target ≥ 120 lines. No emoji inflation; facts + pixel-level references.
  </action>
  <verify>
    <automated>test -s /home/cae/ctrl-alt-elite/dashboard/.planning/phases/13-ui-ux-review-polish-loop/audit/UI-AUDIT-visual-pillars.md && wc -l /home/cae/ctrl-alt-elite/dashboard/.planning/phases/13-ui-ux-review-polish-loop/audit/UI-AUDIT-visual-pillars.md | awk '{ exit ($1 >= 120) ? 0 : 1 }' && grep -q "P0\|P1\|P2" /home/cae/ctrl-alt-elite/dashboard/.planning/phases/13-ui-ux-review-polish-loop/audit/UI-AUDIT-visual-pillars.md && grep -Ec "hierarchy|density|consistency|motion|typography|color" /home/cae/ctrl-alt-elite/dashboard/.planning/phases/13-ui-ux-review-polish-loop/audit/UI-AUDIT-visual-pillars.md | awk '{ exit ($1 >= 6) ? 0 : 1 }'</automated>
  </verify>
  <done>
  - audit/UI-AUDIT-visual-pillars.md ≥ 120 lines
  - Every shipped surface has a row with pillar scores
  - Findings ranked P0/P1/P2 with plan assignment
  - Cross-surface patterns consolidated (not duplicated across 12 rows)
  - Plans 13-10 and 13-11 have a clear subset to work from
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix top nav + alert banner + cluster chips — text-dim replacement + density + accent restraint</name>
  <files>app/globals.css, tailwind.config.ts, components/shell/top-nav.tsx, components/shell/cost-ticker.tsx, components/shell/heartbeat-dot.tsx, components/shell/liveness-chip.tsx, components/shell/alert-banner.tsx, components/shell/ambient-clock.tsx, lib/copy/labels.ts</files>
  <action>
  1. **text-dim body fix** (pillar 6, WCAG SC 1.4.3):
     - `grep -rn "text-\[color:var(--text-dim)\]\|text-dim" components/shell/ components/build-home/ app/build/ 2>/dev/null` — every hit on body copy changes to `text-muted`
     - Preserve text-dim for meta labels ≤10px tabular-nums (LastUpdated primitive uses `text-[color:var(--text-muted)]` already per plan 13-06 — verify no regression)
     - If LastUpdated OR other 10px mono chips uses text-dim on actual user-visible digits, upgrade to text-muted
  2. **Density / 8pt grid sweep:**
     - `grep -rE "p-\[|m-\[|gap-\[|space-[xy]-\[" components/shell/ components/build-home/` — every arbitrary value gets mapped to 8pt grid equivalent
     - Top-nav spacing: `gap-3` between cluster chips, `px-4 py-2` on container — verify per UI-SPEC §13
  3. **Accent restraint** (pillar 6, V2 §6 "accent used for >5 roles on one screen = flag"):
     - Count accent-colored elements on top-nav screenshot → if >3, demote decorative uses to text-muted/text
     - Retain accent for: active mode chip (Build/Plan toggle), primary CTA (if any), focus ring
  4. **Motion respect** (pillar 4):
     - In LivenessChip + AmbientClock, wrap the setInterval inside `if (!prefersReducedMotion)` or set interval to 60000 when reduced
     - Pulse animations on heartbeat-dot: verify `@media (prefers-reduced-motion: reduce) { animation: none; }` in globals.css
  5. **Typography** (pillar 5):
     - `grep -rn "font-\[" components/shell/` — any leaked fallback font-family gets replaced with Geist Sans or Geist Mono
     - Font sizes must be in {13,14,15,16,20,24,32}px — any arbitrary size gets rounded
  6. **AlertBanner polish:**
     - Review plan 13-07 output; verify amber border + amber-900/10 bg + var(--text) copy (not dim)
     - CTA buttons: size 24×24 minimum per WCAG 2.5.8
     - Dismiss X at 24×24 with aria-label="Dismiss alert"
  7. `pnpm build` + `pnpm test --run` both green.
  8. Re-capture top-nav strips:
     ```bash
     cd /home/cae/ctrl-alt-elite/dashboard/.planning/phases/13-ui-ux-review-polish-loop/scripts
     bash capture.sh after --route build-home --viewport laptop --mode founder
     bash capture.sh after --route build-home --viewport mobile --mode founder
     ```
     Diff quickly against before/ PNGs to sanity-check fixes landed.

  Commit: `refactor(13-09): top nav + alert banner pillar 5/6 fixes — text-dim → text-muted, 8pt grid, accent restraint`
  </action>
  <verify>
    <automated>cd /home/cae/ctrl-alt-elite/dashboard && grep -rE "text-\[color:var\(--text-dim\)\]" components/shell/ components/build-home/ | grep -v "test." | wc -l | awk '{ exit ($1 <= 2) ? 0 : 1 }' && pnpm build 2>&1 | tail -3 | grep -qE "Compiled|ready" && pnpm test --run 2>&1 | tail -5 | grep -qE "passed"</automated>
  </verify>
  <done>
  - ≤2 text-dim usages remaining in touched surfaces (tiny 10px meta chips only)
  - 8pt grid respected: no arbitrary p-[Xpx] in touched files
  - Motion: LivenessChip + AmbientClock pause under prefers-reduced-motion
  - pnpm build + test green
  - After-shot smoke captured for at least 2 build-home viewports
  </done>
</task>

<task type="auto">
  <name>Task 3: Rollup strip → MC-style card treatment + build-home pillar fixes</name>
  <files>components/build-home/rollup-strip.tsx, components/build-home/active-phase-cards.tsx, components/build-home/needs-you-list.tsx, components/build-home/recent-ledger.tsx, components/build-home/live-ops-line.tsx, app/build/page.tsx, .planning/phases/13-ui-ux-review-polish-loop/audit/UI-AUDIT-visual-top-and-home.md</files>
  <action>
  1. **Rewrite rollup-strip.tsx to card-grid treatment** per interfaces section. Icons from Lucide (`pnpm add lucide-react` if not installed — verify first). Keep existing data source (no new fetch). Test: render with stub data; assert 5 cards; assert icons render; assert amber dot iff slot.warning.
  2. **Fix V2 pitfall #2**: "Warnings: 0" slot — ensure dot uses `text-dim` color (not amber) when value === 0. Same for all "counter" cards — amber only when > 0.
  3. **Fix V2 pitfall #3** (empty-state distinguishability):
     - `needs-you-list.tsx` empty state: "Nothing needs you right now" → visually distinct from loading: add `<Check/>` icon in dim color, explicit "everything clear" semantic, NOT just a dash
     - `recent-ledger.tsx` empty state: "No activity yet today" with clock icon
     - `live-ops-line.tsx` empty state: "Idle" with subtle dot — different from loading skeleton
  4. **active-phase-cards.tsx polish:**
     - Verify progress bar + wave text use pillar-2 spacing
     - `progress_pct` numeric goes through `tabular-nums`
     - Phase title = 16px semibold (hero for card); subtitle = 13px muted
  5. **live-ops-line.tsx:** convert plain-text announcement to a proper row with icon + mono timestamp + sentence — this is Eric's most-seen line; polish matters
  6. **app/build/page.tsx layout sweep:** verify 8pt gaps between rollup strip, active phases, needs-you, recent ledger sections. Use `gap-6` or `gap-8`, not arbitrary.
  7. **Write `audit/UI-AUDIT-visual-top-and-home.md`** — the plan-13-09-specific output documenting what was in-scope, what fixed, what deferred:
     - Pillar scores before/after per surface
     - Deferred items referencing plans 13-10/11 (e.g. agents grid consistency — 13-10 handles that)
     - Screenshot paths: shots/before/.../build-home.png vs shots/after-13-09/.../build-home.png if available
     - Remaining risks for Wave 7 delta: rollup cards on mobile (grid-cols-2) may feel cramped — flag
  8. Re-run `pnpm build` + `pnpm test` + smoke /build on :3003.

  Commit: `refactor(13-09): build-home pillar polish + rollup card grid treatment (MC §5 row 3)`
  </action>
  <verify>
    <automated>cd /home/cae/ctrl-alt-elite/dashboard && grep -q "grid-cols-" components/build-home/rollup-strip.tsx && grep -q "lucide-react" package.json && test -s .planning/phases/13-ui-ux-review-polish-loop/audit/UI-AUDIT-visual-top-and-home.md && wc -l .planning/phases/13-ui-ux-review-polish-loop/audit/UI-AUDIT-visual-top-and-home.md | awk '{ exit ($1 >= 60) ? 0 : 1 }' && pnpm test --run 2>&1 | tail -5 | grep -qE "passed" && pnpm build 2>&1 | tail -3 | grep -qE "Compiled|ready"</automated>
  </verify>
  <done>
  - Rollup strip renders as 5-card grid with icons + colored dots
  - "Warnings: 0" dot is text-dim (not amber) when value=0
  - Empty states on needs-you / recent-ledger / live-ops clearly distinguishable from loading
  - active-phase-cards respects pillar-2 spacing + pillar-5 typography
  - UI-AUDIT-visual-top-and-home.md ≥ 60 lines with before/after pillar scores
  - pnpm build + test green
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new — pure UI polish.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-09-01 | DoS | Per-second ticking clock + LastUpdated | mitigate | Reduced-motion already pauses ticking per plan 13-06/07; respected here |
| T-13-09-02 | Tampering | Lucide icon import surface | accept | Established library; already used in other parts of codebase (grep) |
</threat_model>

<verification>
- UI-AUDIT-visual-pillars.md ≥ 120 lines (master doc for 13-10/11 too)
- UI-AUDIT-visual-top-and-home.md ≥ 60 lines (this plan's scope)
- text-dim usage on body copy: 0 remaining in touched surfaces
- Rollup strip is a card grid with Lucide icons
- pnpm build + test all green
</verification>

<success_criteria>
- Top nav + build home + alert banner score ≥3/4 on every pillar
- WCAG SC 1.4.3 satisfied on touched surfaces
- Plans 13-10/11 can inherit the pillar audit and focus on their surfaces
</success_criteria>

<output>
Create `.planning/phases/13-ui-ux-review-polish-loop/13-09-SUMMARY.md`:
- File change counts + summary per pillar
- Before/after pillar scores for top nav + build home + alert banner
- Any P0 leftover flagged for plan 13-10 or 13-11
- Rollup card responsive check: mobile, laptop, wide
</output>
