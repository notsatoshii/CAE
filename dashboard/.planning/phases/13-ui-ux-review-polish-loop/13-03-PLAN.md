---
phase: 13-ui-ux-review-polish-loop
plan: 03
type: execute
wave: 1.5
depends_on: [13-01, 13-02]
files_modified:
  - .planning/phases/13-ui-ux-review-polish-loop/scripts/verify.py
  - .planning/phases/13-ui-ux-review-polish-loop/audit/UI-AUDIT-correctness.md
  - .planning/phases/13-ui-ux-review-polish-loop/audit/VERIFY.md
autonomous: true
requirements:
  - REQ-P13-02

must_haves:
  truths:
    - "Every panel in V2 §1 source-of-truth map has a verify.py row comparing API output to source-of-truth file"
    - "For every panel: verdict is ✅ match or ❌ MISMATCH with both formatted numbers shown"
    - "The ranked P0/P1/P2 data-correctness finding list in UI-AUDIT-correctness.md is grounded in verify.py output, not hunches"
    - "WR-01 (chat unread-count SSE ID bug) is confirmed in situ as a concrete ❌ finding ready for Wave 2 fix (plan 13-04)"
  artifacts:
    - path: ".planning/phases/13-ui-ux-review-polish-loop/scripts/verify.py"
      provides: "Extended to cover all ≥12 panels from V2 §1 map, not just cost ticker"
      min_lines: 250
      contains: "PANELS"
    - path: ".planning/phases/13-ui-ux-review-polish-loop/audit/VERIFY.md"
      provides: "Machine-generated three-way verification table — regenerated on every verify.py run"
      contains: "MISMATCH"
    - path: ".planning/phases/13-ui-ux-review-polish-loop/audit/UI-AUDIT-correctness.md"
      provides: "Human-read audit findings ranked P0→P2, each with source of truth, observed rendered value, fix sketch, and file:line evidence"
      min_lines: 80
  key_links:
    - from: "verify.py PANELS list"
      to: "/api/state, /api/agents, /api/metrics, /api/changes, /api/queue, /api/memory/tree, /api/chat/state"
      via: "urllib.request with auth cookie"
      pattern: "api("
    - from: "verify.py source_fn"
      to: ".cae/metrics/*.jsonl + .planning/phases/* + git log"
      via: "direct file read + subprocess for git"
      pattern: "tail_jsonl|git log"
---

<objective>
Expand the `verify.py` scaffold from Wave 0 to cover every panel in V2 §1 source-of-truth map, run it, and produce a ranked P0/P1/P2 audit report (`UI-AUDIT-correctness.md`) with concrete fix sketches.

This is the core of Phase 13's correctness-first thesis: before we "polish" anything, we prove which numbers are wrong, why, and what to do about it. V2 research predicts 5-8 P0 mismatches; the audit either finds them or explicitly clears the panels.

Purpose: Wave 2 (plan 13-04 WR-01 fix) + Wave 6 fix plans (13-09/10/11) consume this audit. Without it, we're polishing a dashboard that lies.

Output: verify.py with ~12 panels, machine-generated VERIFY.md table, human-written UI-AUDIT-correctness.md with ranked findings + fix sketches + file:line evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-ui-ux-review-polish-loop/13-CONTEXT.md
@.planning/phases/13-ui-ux-review-polish-loop/13-RESEARCH-V2.md
@.planning/phases/13-ui-ux-review-polish-loop/13-ERIC-CRITIQUE.md
@.planning/phases/13-ui-ux-review-polish-loop/13-01-PLAN.md
@.planning/phases/13-ui-ux-review-polish-loop/13-02-PLAN.md
@.planning/phases/13-ui-ux-review-polish-loop/audit/BASELINE.md
@.planning/phases/13-ui-ux-review-polish-loop/scripts/verify.py
@lib/cae-home-state.ts
@lib/cae-metrics-state.ts
@lib/cae-changes-state.ts
@lib/cae-chat-state.ts
@lib/cae-agents-state.ts
@lib/cae-queue-state.ts
@app/api/state/route.ts
@app/api/agents/route.ts
@app/api/metrics/route.ts
@app/api/changes/route.ts
@app/api/queue/route.ts
@app/api/memory/tree/route.ts
@app/api/chat/state/route.ts
@app/api/chat/send/route.ts
@components/shell/cost-ticker.tsx
@components/shell/heartbeat-dot.tsx
@components/build-home/rollup-strip.tsx
@components/build-home/active-phase-cards.tsx
@components/build-home/needs-you-list.tsx
@components/build-home/recent-ledger.tsx
@components/chat/chat-rail.tsx

<interfaces>
<!-- V2 §1 source-of-truth map (grep-verified 2026-04-23). Treat as the PANELS target list. -->

| Panel | Component | API | Source of truth | Notes |
|-------|-----------|-----|-----------------|-------|
| Cost ticker | shell/cost-ticker.tsx | /api/state .breakers.inputTokensToday/outputTokensToday | sum input+output_tokens from .cae/metrics/circuit-breakers.jsonl where ts startsWith today | Already drafted in 13-01 scaffold |
| Heartbeat dot | shell/heartbeat-dot.tsx | /api/state .breakers.halted/retryCount/recentPhantomEscalations | grep forge_end.jsonl success:false count | Aggregator derives retryCount |
| Rollup shipped_today | build-home/rollup-strip.tsx | /api/state .rollup.shipped_today | count of sentinel.jsonl rows type=merge today | cae-home-state.ts line ~200-400 |
| Rollup tokens_today | same | /api/state .rollup.tokens_today | same as cost ticker sum | must match cost ticker within ±0 |
| Rollup in_flight | same | /api/state .rollup.in_flight | count of forge_start without matching forge_end | heuristic — verify vs actual tmux panes |
| Rollup blocked | same | /api/state .rollup.blocked | count of needs_you items type=blocked | must equal needs_you.length where type=blocked |
| Rollup warnings | same | /api/state .rollup.warnings | breakers.retryCount + breakers.recentPhantomEscalations | |
| Active phase wave_current | build-home/active-phase-cards.tsx | /api/state .home_phases[].wave_current | parse .planning/phases/NN-*/PLAN.md frontmatter max completed wave + 1 | cae-home-state.ts ~line 500 |
| Active phase progress_pct | same | /api/state .home_phases[].progress_pct | completed task count / total task count | drift-prone per V2 pre-finding |
| Needs-you count | build-home/needs-you-list.tsx | /api/state .needs_you[] length | sum of: blocked circuit breakers + pending plan-reviews + dangerous-action gates | |
| Recent ledger token sums | build-home/recent-ledger.tsx | /api/state .events_recent[].tokens | sum forge_end tokens per event id | 20 rows |
| Agents 7d success % | build-home/agent-avatars.tsx + /api/agents | /api/agents .[i].success_rate_7d | forge_end rows past 7d where success=true / total forge_end past 7d per agent | lib/cae-agents-state.ts |
| Metrics MTD spend | /metrics | /api/metrics .spend_mtd | sum tokens-as-cost for current month from circuit-breakers.jsonl | lib/cae-metrics-state.ts |
| Changes timeline counts | /build/changes | /api/changes .merges_today | count sentinel.jsonl merge events today | |
| Queue counts | /build/queue | /api/queue | listInbox() + listOutbox() grouped by status | lib/cae-queue-state.ts |
| Memory tree leaf count | /memory | /api/memory/tree | fs walk of AGENTS.md + KNOWLEDGE/ + .claude/agents/ + .planning/phases/*/ files | lib/cae-memory-tree.ts |
| Chat unread count | chat/chat-rail.tsx | /api/chat/state .unread | readTranscriptAfter(lastSeenMsgId).length across sessions | **WR-01 CONFIRMED BROKEN** per V2 §1 |

Pre-confirmed findings (should be surfaced by this audit, not discovered):
- **WR-01** — app/api/chat/send/route.ts:165/213/222/273 — randomUUID() per SSE frame overwrites client lastSeenMsgId → unreadCount always 0 after reload. P0.
- (possible) Rollup tokens_today ≠ cost ticker tokensToday by a formatter rounding quirk — flag if discovered.
- (possible) cae-home-state.ts wave_current derivation off-by-one — Eric's "details incorrect".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand verify.py to cover all 17 panels from V2 §1 + run it</name>
  <files>.planning/phases/13-ui-ux-review-polish-loop/scripts/verify.py, .planning/phases/13-ui-ux-review-polish-loop/audit/VERIFY.md</files>
  <action>
  For EACH row in the §1 source-of-truth map above, add a PANELS entry to verify.py matching the cost-ticker pattern from 13-01:
  ```python
  PANELS.append({
      "panel": "...",
      "api_path": "/api/...",
      "api_getter": lambda j: {...},
      "source_fn": lambda: {...},   # computes expected from source files
      "formatter": lambda d: f"...", # stable string for equality compare
      "tolerance": 0,
  })
  ```

  Implementation guide per panel (read the actual aggregator files first — don't guess):
  1. **Rollup shipped_today** — `source_fn` counts `sentinel.jsonl` rows where `type == "merge"` and `ts` starts with today. Compare to `api_getter(j)["rollup"]["shipped_today"]`.
  2. **Rollup tokens_today** — reuse cost_ticker_source() but sum both fields; compare to `j["rollup"]["tokens_today"]`.
  3. **Rollup in_flight** — grep `forge_start.jsonl` where no matching `forge_end.jsonl` row exists (match on correlation id or ts ordering per lib/cae-home-state.ts logic); compare to `j["rollup"]["in_flight"]`.
  4. **Rollup blocked** — filter `j["needs_you"]` where `type == "blocked"`, compare its length to `j["rollup"]["blocked"]`. If the API says 5 blocked but needs_you list is 3, that's a self-inconsistent aggregator bug — P0.
  5. **Rollup warnings** — `breakers.retryCount + breakers.recentPhantomEscalations`; compare to `j["rollup"]["warnings"]`.
  6. **Active phase wave_current** — For each `home_phase`, read `.planning/phases/{phase_dir}/*-PLAN.md` frontmatter + SUMMARY.md presence to derive expected wave_current per cae-home-state.ts logic (inspect the aggregator's source for the exact rule). Compare.
  7. **Active phase progress_pct** — count `- [x]` vs `- [ ]` in the SUMMARY/PLAN files (follow the aggregator's rule); compare to `j["home_phases"][i]["progress_pct"]`.
  8. **Needs-you count** — derived from V2 §1: sum of blocked circuit breakers (halted=true OR retryCount>0) + pending plan-reviews (`.planning/**/PLAN.md` without SUMMARY) + dangerous gates (queue inbox items with flag). Compare to `len(j["needs_you"])`.
  9. **Recent ledger token sums** — pull `events_recent[:20]` from /api/state, find matching `forge_end.jsonl` rows by id, compare summed tokens.
  10. **Agents 7d success %** — Call `/api/agents`. For each agent, read `forge_end.jsonl` rows in last 7d filtered by agent name; `success_rate = passed/total * 100`. Compare with `api_obj["success_rate_7d"]` at ±1% tolerance.
  11. **Metrics MTD spend** — `/api/metrics` → `spend_mtd`. Source = sum of (input_tokens + output_tokens) × per-model rate, month-to-date. If aggregator uses a table, read the same table — don't reinvent pricing.
  12. **Changes merges_today** — `/api/changes` → count of git log --merges with committer-date today; compare to what API returns.
  13. **Queue counts** — `/api/queue` returns buckets; source = `listInbox() + listOutbox()` counts (read `.cae/chat/inbox/` or whatever `lib/cae-queue-state.ts` tails — verify by grepping).
  14. **Memory tree leaf count** — `/api/memory/tree` → walk count; source = `fs.walk` on the same paths the aggregator walks.
  15. **Chat unread count (WR-01)** — Trigger it deliberately: Playwright sign-in, open /chat, send "hello", reload, read `/api/chat/state` unread — expect non-zero, source-of-truth is `readTranscriptAfter(lastSeenMsgId)` where lastSeenMsgId is the last persisted assistant message id. This panel WILL fail per V2 pre-finding — its row becomes Finding #1.

  When a `source_fn` requires reading large files (cae-home-state.ts is 725 lines), read it in-process and compute — don't shell out to `cae` or re-run the aggregator (circularity). The point is a SECOND-OPINION verifier.

  Keep formatters stable — if /api rounds to 1 decimal place and source is integer, round source to match. If they disagree because of rounding, that's a ⚠️ "rounding mismatch" not a ❌ "data wrong" — add a distinct verdict.

  After all 17 panels added, execute:
  ```bash
  cd /home/cae/ctrl-alt-elite/dashboard/.planning/phases/13-ui-ux-review-polish-loop/scripts
  python3 verify.py 2>&1 | tee ../audit/working/verify-run.log
  ```
  Expect: `audit/VERIFY.md` table with 17 rows, exit code 1 if ≥1 ❌ (expected per V2 pre-findings).

  If an aggregator's derivation rule is opaque enough that writing a second-opinion `source_fn` is itself risky, document that panel as `⚠️ UNVERIFIABLE — derivation too complex to second-source without re-implementing aggregator` and flag for human review. This is better than a false-positive ❌.
  </action>
  <verify>
    <automated>cd /home/cae/ctrl-alt-elite/dashboard/.planning/phases/13-ui-ux-review-polish-loop/scripts && grep -c 'PANELS.append' verify.py | awk '{ exit ($1 >= 12) ? 0 : 1 }' && (python3 verify.py || true) && test -s ../audit/VERIFY.md && grep -Ec "✅|❌|⚠️" ../audit/VERIFY.md | awk '{ exit ($1 >= 12) ? 0 : 1 }'</automated>
  </verify>
  <done>
  - verify.py has ≥12 PANELS entries covering all §1 panels
  - `audit/VERIFY.md` generated with ≥12 rows, verdicts assigned
  - At least WR-01 chat unread is present as ❌ MISMATCH row
  - Script exits non-zero if any panel mismatches (expected)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write UI-AUDIT-correctness.md — ranked P0/P1/P2 findings with fix sketches</name>
  <files>.planning/phases/13-ui-ux-review-polish-loop/audit/UI-AUDIT-correctness.md</files>
  <behavior>
    - Every ❌ row in VERIFY.md is promoted to a Finding entry
    - Every ⚠️ UNVERIFIABLE row is noted as a lower-priority follow-up
    - Finding structure: `### Finding N: <title>` + Severity + Source of truth + Rendered value + Root-cause hypothesis + Fix sketch (file:line) + Which plan fixes it
    - Findings ranked P0 (data wrong, user-visible) → P1 (stale or mis-rounded) → P2 (self-consistent but semantically odd)
    - WR-01 is Finding #1 with full file:line + V2 §1 fix sketch pasted verbatim
    - Section "Unverified panels" lists ⚠️ rows with why-opaque + suggestion (e.g. add DEBUG log to aggregator)
  </behavior>
  <action>
  Read `audit/VERIFY.md` and group rows by verdict. For each ❌:
  1. Hypothesize root cause (open the component + its aggregator file, find the derivation)
  2. Write fix sketch with file:line reference
  3. Assign severity: P0 if user sees wrong number (cost ticker, rollup, unread count, phase progress); P1 if stale/rounded; P2 if semantic only
  4. Map to a plan: WR-01 → plan 13-04; aggregator drifts → plans 13-09/10/11 (Wave 6 fix plans); logging-dependent ones → plan 13-05

  Example entry:
  ```markdown
  ### Finding 1: Chat unread count resets to 0 on every page reload (WR-01)

  **Severity:** P0
  **Source of truth:** `.cae/chat/sessions/*.jsonl` — last persisted assistant `msg_id` per session
  **Rendered value:** 0 (always, after any reload)
  **Expected value:** count of messages newer than `lastSeenMsgId` across active sessions
  **Evidence:**
  - `app/api/chat/send/route.ts:165` emits `randomUUID()` on every SSE `assistant.begin` frame
  - Line 213: every `assistant.delta` gets a fresh `randomUUID()` (wrong — should be same id as `beginId` or empty)
  - Line 222: `unread_tick` gets `randomUUID()` (wrong — no id should overwrite lastSeenMsgId on a non-message event)
  - Line 273: final persisted `assistantMsgId = randomUUID()` — NEVER the same as `beginId`
  - Client (`components/chat/chat-rail.tsx`) persists whichever id was last seen — almost always an ephemeral `unread_tick` UUID that never exists in the transcript
  - `readTranscriptAfter(ephemeralId)` returns `[]` → unread always 0

  **Fix sketch (per V2 §1 — 20 lines):**
  - Compute `assistantMsgId` ONCE at stream start (line 165), use it for `assistant.begin`
  - `assistant.delta` emits empty `id` ("")
  - `unread_tick` emits empty `id` ("") OR rename the SSE field so client doesn't overwrite lastSeenMsgId from it
  - `assistant.end` emits `id = assistantMsgId`; this is the ONLY frame client should promote to lastSeenMsgId
  - Add unit test: send a stream, reload, expect unread >= 1

  **Fixed by:** plan 13-04 (Wave 2)
  ```

  After all ❌, list ⚠️ UNVERIFIABLE panels. Then a "Backlog for future phases" section for P2 items we explicitly defer.

  Length target ≥ 80 lines. Be critical, not apologetic. If zero ❌ findings materialize (unlikely per V2 pre-findings), write that explicitly and reflect on whether the audit was thorough enough.
  </action>
  <verify>
    <automated>test -s /home/cae/ctrl-alt-elite/dashboard/.planning/phases/13-ui-ux-review-polish-loop/audit/UI-AUDIT-correctness.md && wc -l /home/cae/ctrl-alt-elite/dashboard/.planning/phases/13-ui-ux-review-polish-loop/audit/UI-AUDIT-correctness.md | awk '{ exit ($1 >= 80) ? 0 : 1 }' && grep -q "WR-01\|Finding 1" /home/cae/ctrl-alt-elite/dashboard/.planning/phases/13-ui-ux-review-polish-loop/audit/UI-AUDIT-correctness.md && grep -qE "P0|P1|P2" /home/cae/ctrl-alt-elite/dashboard/.planning/phases/13-ui-ux-review-polish-loop/audit/UI-AUDIT-correctness.md</automated>
  </verify>
  <done>
  - `UI-AUDIT-correctness.md` ≥ 80 lines
  - WR-01 listed as Finding 1 with file:line evidence + V2 fix sketch
  - Every ❌ from VERIFY.md has a finding entry with severity + fix sketch + plan mapping
  - ⚠️ UNVERIFIABLE panels documented separately
  - Output is grounded in actual verify.py output, not hunches
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Inherited. `verify.py` gains new read paths into `.cae/metrics/*.jsonl` and `.planning/phases/*` — all local fs, same trust as scripts already enjoy.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03-01 | Tampering | verify.py `source_fn` re-implementing aggregator logic | mitigate | Explicitly a second-opinion verifier — mismatches require human judgment before declaring the aggregator wrong; ⚠️ UNVERIFIABLE verdict available for opaque derivations |
| T-13-03-02 | Information Disclosure | VERIFY.md may leak exact token counts | accept | Local-only, gitignored under audit/ if we want; but audit/*.md are tracked — if token counts sensitive, move VERIFY.md to audit/working/ |
</threat_model>

<verification>
- verify.py runs to completion and writes VERIFY.md with ≥12 verdicts
- UI-AUDIT-correctness.md lists findings with severity + fix sketch + plan mapping
- WR-01 finding matches V2 §1 evidence exactly
- No production code modified
</verification>

<success_criteria>
- All ≥12 panels from V2 §1 verified
- At least WR-01 surfaces as ❌ (if not, verify.py is wrong — re-examine chat stream harness)
- Audit report ranked P0/P1/P2 with concrete fix locations
</success_criteria>

<output>
Create `.planning/phases/13-ui-ux-review-polish-loop/13-03-SUMMARY.md`:
- Panel coverage count (target 17, accept 12+)
- Mismatch count + severity breakdown
- Pre-confirmed findings that DIDN'T surface (e.g., if WR-01 somehow didn't fire — that's itself a finding about the harness)
- Which plans downstream consume which findings
</output>
