---
phase: 13-ui-ux-review-polish-loop
plan: 11
type: execute
wave: 6
depends_on: [13-09]
files_modified:
  - app/memory/page.tsx
  - components/memory/browse/tree.tsx
  - components/memory/graph/graph-canvas.tsx
  - components/memory/graph/regenerate-button.tsx
  - components/memory/why-drawer.tsx
  - components/memory/timeline.tsx
  - app/metrics/page.tsx
  - components/metrics/spending-panel.tsx
  - components/metrics/howwell-panel.tsx
  - components/metrics/howfast-panel.tsx
  - components/shell/incident-stream.tsx
  - app/chat/page.tsx
  - components/chat/chat-rail.tsx
  - components/chat/chat-panel.tsx
  - components/chat/message.tsx
  - app/plan/page.tsx
  - app/signin/page.tsx
  - .planning/phases/13-ui-ux-review-polish-loop/audit/UI-AUDIT-visual-memory-metrics-chat-plan.md
autonomous: true
requirements:
  - REQ-P13-07
  - REQ-P13-10

must_haves:
  truths:
    - "/memory (browse + graph tabs + drawers), /metrics, /chat, /plan, /signin all score ≥ 3/4 on every pillar"
    - "Memory tree uses consistent indentation (16px per level) + Lucide chevrons + tabular-nums file counts"
    - "Memory graph empty state is visually distinct from 'loading' and 'regeneration-cooldown'"
    - "Metrics panels have unified chrome (same border, same padding, same header pattern) matching Incident Stream panel per plan 13-08"
    - "Chat message bubbles respect pillar-3 consistency — user/assistant distinct but both follow same max-width + padding rules"
    - "Signin page has visual polish expected of a founder-first product — no default Next.js placeholder vibes"
  artifacts:
    - path: ".planning/phases/13-ui-ux-review-polish-loop/audit/UI-AUDIT-visual-memory-metrics-chat-plan.md"
      provides: "Pillar audit + fix list for the 4 surfaces in scope (memory is 1 route with 2+ states; metrics+chat+plan+signin each 1)"
      min_lines: 70
  key_links:
    - from: "audit/UI-AUDIT-visual-pillars.md (from plan 13-09)"
      to: "rows for /memory, /metrics, /chat, /plan, /signin"
      via: "pillar scores"
      pattern: "/memory|/metrics|/chat|/plan|/signin"
    - from: "components/shell/incident-stream.tsx chrome (from plan 13-08)"
      to: "components/metrics/*.tsx panel chrome"
      via: "shared class pattern"
      pattern: "rounded-lg border"
---

<objective>
Wave 6 Part 3 — visual pillar polish for the four remaining surfaces: memory (browse + graph + why-drawer + timeline), metrics (3 panels + incident stream), chat (rail + panel + /chat page), plan home, and signin.

Purpose: closes the pillar-score gap on less-frequently-visited but still-critical surfaces. signin in particular is the first impression for any future collaborator; must not look placeholder.

Output: 5 surfaces polished + 1 audit doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-ui-ux-review-polish-loop/13-RESEARCH-V2.md
@.planning/phases/13-ui-ux-review-polish-loop/audit/UI-AUDIT-visual-pillars.md
@.planning/phases/13-ui-ux-review-polish-loop/shots/before/laptop-founder/memory-browse.png
@.planning/phases/13-ui-ux-review-polish-loop/shots/before/laptop-founder/memory-graph.png
@.planning/phases/13-ui-ux-review-polish-loop/shots/before/laptop-founder/memory-why-drawer.png
@.planning/phases/13-ui-ux-review-polish-loop/shots/before/laptop-founder/memory-timeline.png
@.planning/phases/13-ui-ux-review-polish-loop/shots/before/laptop-founder/metrics.png
@.planning/phases/13-ui-ux-review-polish-loop/shots/before/laptop-founder/chat.png
@.planning/phases/13-ui-ux-review-polish-loop/shots/before/laptop-founder/plan.png
@.planning/phases/13-ui-ux-review-polish-loop/shots/before/laptop-founder/signin.png
@.planning/phases/13-ui-ux-review-polish-loop/shots/before/mobile-founder/memory-browse.png
@.planning/phases/13-ui-ux-review-polish-loop/shots/before/mobile-founder/metrics.png
@.planning/phases/13-ui-ux-review-polish-loop/shots/before/mobile-founder/chat.png
@docs/UI-SPEC.md
@app/memory/page.tsx
@app/metrics/page.tsx
@app/chat/page.tsx
@app/plan/page.tsx
@app/signin/page.tsx
@components/memory/browse/tree.tsx
@components/memory/graph/graph-canvas.tsx
@components/chat/chat-rail.tsx
@components/chat/chat-panel.tsx
@components/chat/message.tsx

<interfaces>
<!-- Memory tree per V2 §6 pillar 2+3 (consistent indentation + Lucide chevrons): -->
```tsx
<li className="list-none">
  <button className="flex w-full items-center gap-1.5 rounded px-2 py-1 text-left text-[13px] hover:bg-[color:var(--bg-elev)]" onClick={toggle}>
    {hasChildren ? (expanded ? <ChevronDown className="size-3.5"/> : <ChevronRight className="size-3.5"/>) : <span className="size-3.5"/>}
    <FileIcon type={node.type} className="size-3.5 shrink-0"/>
    <span className="truncate">{node.name}</span>
    {node.fileCount != null && <span className="ml-auto shrink-0 font-mono text-[11px] text-[color:var(--text-muted)] tabular-nums">{node.fileCount}</span>}
  </button>
  {expanded && <ul className="ml-4">{children}</ul>}
</li>
```

<!-- Memory graph empty state distinct from cooldown vs loading: -->
- Empty: "No graph data yet. Run regenerate to build one." + big Regenerate CTA
- Cooldown (regenerate ran < 60s ago): "Regeneration complete. Refreshing in {n}s." + progress indicator
- Loading (first render): skeleton shimmer

<!-- Metrics + incident stream panel chrome unification: -->
```tsx
// Shared Panel component (components/ui/panel.tsx if not yet present):
<section className="rounded-lg border border-[color:var(--border)] bg-[color:var(--bg-elev)] p-6">
  <header className="mb-4 flex items-baseline justify-between gap-2">
    <h2 className="text-[15px] font-semibold">{title}</h2>
    {subtitle && <span className="text-[12px] text-[color:var(--text-muted)]">{subtitle}</span>}
  </header>
  {children}
</section>
```
Refactor spending / how-well / how-fast / incident-stream panels to use this `<Panel/>` wrapper. Reduces inconsistency (pillar 3).

<!-- Chat message bubble pillar-3 rules: -->
- User + assistant both: max-width ~65ch (pillar 5 measure), rounded-lg, p-3, text-15px
- User: right-aligned, `bg-accent/10 border border-accent/20`
- Assistant: left-aligned, `bg-bg-elev border border-border`
- Avatar (persona icon) outside bubble for assistant
- Time in 11px mono below bubble, same color muted for both roles

<!-- Signin polish -->
Current signin is near-default. Ship:
- Centered card (max-width 24rem)
- Product name + 1-line value prop
- GitHub sign-in button prominently styled (brand-appropriate but matches our cyan accent)
- Subtle background pattern OR gradient (UI-SPEC-compliant, not distracting)
- Footer with tiny version + year
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Memory tree + graph + drawers polish</name>
  <files>app/memory/page.tsx, components/memory/browse/tree.tsx, components/memory/graph/graph-canvas.tsx, components/memory/graph/regenerate-button.tsx, components/memory/why-drawer.tsx, components/memory/timeline.tsx</files>
  <action>
  1. **tree.tsx per interfaces template:** Lucide ChevronRight/ChevronDown, FileIcon component (map node.type → icon), tabular-nums file counts, 16px indent per level (`ml-4`). Smooth expand/collapse with `prefers-reduced-motion` respect.
  2. **graph-canvas.tsx:** empty state + cooldown + loading clearly distinct. Cooldown progress bar uses `--accent`. Empty CTA prominent.
  3. **regenerate-button.tsx:** countdown was already shipping (plan 13-06 LastUpdated similar pattern); double-check its freshness indicator + ensure 24×24 click target.
  4. **why-drawer.tsx + timeline.tsx:** pillar-5 typography audit — h1 = 20px semibold, body = 15px, mono for file paths 12-13px. Pillar-2 padding: 24-32px drawer padding per UI-SPEC.
  5. **app/memory/page.tsx tabs:** browse/graph tabs use shared Tabs primitive (grep for existing tabs in codebase); ensure tab label + underline accent consistent.
  6. `pnpm test --run components/memory` green
  7. Smoke /memory + /memory?tab=graph on :3003

  Commit: `refactor(13-11): memory tree + graph + drawer pillar polish`
  </action>
  <verify>
    <automated>cd /home/cae/ctrl-alt-elite/dashboard && grep -q "ChevronDown\|ChevronRight" components/memory/browse/tree.tsx && pnpm test --run components/memory 2>&1 | tail -5 | grep -qE "passed" && pnpm build 2>&1 | tail -3 | grep -qE "Compiled|ready"</automated>
  </verify>
  <done>
  - Memory tree uses Lucide chevrons + consistent indent + tabular-nums counts
  - Graph empty/cooldown/loading states distinct
  - Drawers respect UI-SPEC §13 padding
  - pnpm build + test green
  </done>
</task>

<task type="auto">
  <name>Task 2: Metrics panels + incident stream — unified Panel chrome + chat polish</name>
  <files>components/ui/panel.tsx, components/ui/panel.test.tsx, app/metrics/page.tsx, components/metrics/spending-panel.tsx, components/metrics/howwell-panel.tsx, components/metrics/howfast-panel.tsx, components/shell/incident-stream.tsx, app/chat/page.tsx, components/chat/chat-rail.tsx, components/chat/chat-panel.tsx, components/chat/message.tsx</files>
  <action>
  1. **Create `components/ui/panel.tsx`** shared wrapper per interfaces section. Test with heading + subtitle + children.
  2. **Refactor spending/howwell/howfast panels** to use `<Panel title subtitle>` (subtitle comes from GoldenSignalsSubtitle on /metrics per plan 13-07).
  3. **Refactor incident-stream.tsx from plan 13-08** to use `<Panel>` wrapper for consistency.
  4. **chat-panel.tsx + message.tsx:**
     - Message bubble rules per interfaces: max-width ~65ch, p-3, rounded-lg, distinct user/assistant chrome
     - Time stamps below bubble in 11px mono muted
     - Avatar icon only on assistant side (persona icon via agent name → Lucide map or per-persona SVG)
     - Empty chat state: "Start a conversation with {persona}" in muted text, centered
  5. **chat-rail.tsx** — after plan 13-04 + 13-06, rail has SSE health dot + LastUpdated; verify these use pillar-3 chrome: same size, same color semantics as elsewhere.
  6. **app/chat/page.tsx:** full-page chat uses same Panel chrome; send box consistent with /build/queue delegate form (pillar 3).
  7. `pnpm test --run components/chat components/metrics components/ui/panel` green

  Commit: `refactor(13-11): shared Panel chrome + chat pillar polish`
  </action>
  <verify>
    <automated>cd /home/cae/ctrl-alt-elite/dashboard && test -s components/ui/panel.tsx && grep -l "Panel" components/metrics/spending-panel.tsx components/metrics/howwell-panel.tsx components/metrics/howfast-panel.tsx components/shell/incident-stream.tsx 2>/dev/null | wc -l | awk '{ exit ($1 >= 3) ? 0 : 1 }' && pnpm test --run components/chat components/metrics components/ui 2>&1 | tail -5 | grep -qE "passed" && pnpm build 2>&1 | tail -3 | grep -qE "Compiled|ready"</automated>
  </verify>
  <done>
  - Shared Panel component exists + is used by ≥3 panel-shaped components
  - Chat message bubbles follow max-width + p-3 + rounded-lg rules
  - pnpm build + test green
  </done>
</task>

<task type="auto">
  <name>Task 3: Plan home + signin polish + audit doc</name>
  <files>app/plan/page.tsx, app/signin/page.tsx, .planning/phases/13-ui-ux-review-polish-loop/audit/UI-AUDIT-visual-memory-metrics-chat-plan.md</files>
  <action>
  1. **plan/page.tsx** — this is the Shift FE placeholder. Even in placeholder state, apply pillar-5 typography + pillar-2 density. The landing copy should reflect "Plan mode coming soon — Projects / PRDs / Roadmaps / UAT tabs" (per ROADMAP.md Phase 10 scope). Use UI-SPEC empty-state pattern: icon + headline + sub-copy. No placeholder-in-placeholder feel.
  2. **signin/page.tsx** — per interfaces: centered card max-w-md, product name ("CAE Dashboard" or a less-corporate variant), 1-line value prop ("Control plane for your AI agent fleet"), prominent GitHub sign-in button with Lucide Github icon, footer with year. Subtle border / shadow. Tailwind classes on 8pt grid.
  3. **Write `audit/UI-AUDIT-visual-memory-metrics-chat-plan.md`** (≥ 70 lines):
     - Pillar scores before/after for 5 surfaces (/memory 2 states + drawers counted once, /metrics, /chat, /plan, /signin)
     - Per-surface fix list with file references
     - Cross-reference UI-AUDIT-visual-pillars.md rows
     - Specifically call out pillar 4 (motion) for /memory graph reducedMotion behavior
     - Signin: call out WCAG AA on the GitHub button (min 4.5:1 on text, 3:1 on border)
  4. `pnpm build` + `pnpm test --run` green
  5. Smoke 5 routes on :3003

  Commit: `refactor(13-11): plan + signin polish + pillar audit doc`
  </action>
  <verify>
    <automated>cd /home/cae/ctrl-alt-elite/dashboard && test -s .planning/phases/13-ui-ux-review-polish-loop/audit/UI-AUDIT-visual-memory-metrics-chat-plan.md && wc -l .planning/phases/13-ui-ux-review-polish-loop/audit/UI-AUDIT-visual-memory-metrics-chat-plan.md | awk '{ exit ($1 >= 70) ? 0 : 1 }' && pnpm test --run 2>&1 | tail -5 | grep -qE "passed" && pnpm build 2>&1 | tail -3 | grep -qE "Compiled|ready"</automated>
  </verify>
  <done>
  - plan + signin polished
  - UI-AUDIT-visual-memory-metrics-chat-plan.md ≥ 70 lines with before/after scores
  - Full test + build green
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Signin is the untrusted→trusted boundary. Polish MUST NOT change the auth flow — only visual chrome.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-11-01 | Spoofing | Signin page misleads user | mitigate | Keep "Sign in with GitHub" + Github icon clear; don't add suspect branding; preserve NextAuth flow |
| T-13-11-02 | Information Disclosure | Memory tree shows file paths | accept | Already visible on current page; no change in surface |
</threat_model>

<verification>
- 5 surfaces polished per pillar rubric
- Shared Panel component used across 3+ panel types
- pnpm build + test green
- Audit doc covers all surfaces with before/after pillar scores
</verification>

<success_criteria>
- All shipped surfaces now ≥3/4 on every pillar
- Consistent Panel chrome on metrics + incident stream
- Signin is founder-ready, not placeholder
</success_criteria>

<output>
Create `.planning/phases/13-ui-ux-review-polish-loop/13-11-SUMMARY.md`:
- Pillar scores for 5 surfaces before/after
- Panel component adoption count
- Wave 7 regression risks (e.g., chat bubble max-width may clip on very narrow mobile — flag)
</output>
