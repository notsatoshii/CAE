---
phase: 10-plan-mode-projects-prds-roadmaps-uat
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - dashboard/.planning/phases/10-plan-mode-projects-prds-roadmaps-uat/__fixtures__/plan/state-idea.json
  - dashboard/.planning/phases/10-plan-mode-projects-prds-roadmaps-uat/__fixtures__/plan/state-prd-drafting.json
  - dashboard/.planning/phases/10-plan-mode-projects-prds-roadmaps-uat/__fixtures__/plan/state-roadmap-ready.json
  - dashboard/.planning/phases/10-plan-mode-projects-prds-roadmaps-uat/__fixtures__/plan/state-waiting-plans.json
  - dashboard/.planning/phases/10-plan-mode-projects-prds-roadmaps-uat/__fixtures__/plan/PRD.md
  - dashboard/.planning/phases/10-plan-mode-projects-prds-roadmaps-uat/__fixtures__/plan/ROADMAP.md
  - dashboard/.planning/phases/10-plan-mode-projects-prds-roadmaps-uat/__fixtures__/plan/dot-env-example.txt
  - dashboard/lib/cae-types.ts
  - dashboard/lib/cae-shift.test.ts
  - dashboard/lib/cae-plan-gen.test.ts
  - dashboard/lib/cae-uat.test.ts
  - dashboard/lib/cae-ship.test.ts
  - dashboard/lib/cae-state.test.ts
  - dashboard/lib/cae-plan-home.test.ts
  - dashboard/.env.example
autonomous: true
requirements:
  - REQ-10-01
  - REQ-10-02
  - REQ-10-03
  - REQ-10-04
  - REQ-10-05
  - REQ-10-06
  - REQ-10-07
  - REQ-10-08
  - REQ-10-09
  - REQ-10-10
user_setup: []

must_haves:
  truths:
    - "Six test files exist with failing suites for every cae-shift / cae-plan-gen / cae-uat / cae-ship / cae-state / cae-plan-home lib under construction"
    - "Shift-shaped fixtures (state.json × 4 lifecycle phases, PRD.md, ROADMAP.md, .env.example) are loadable by tests"
    - "Project type has optional shiftPhase + shiftUpdated fields typed as `string | null`"
    - "`.env.example` ships SHIFT_PROJECTS_HOME=/home/cae line"
  artifacts:
    - path: "dashboard/.planning/phases/10-plan-mode-projects-prds-roadmaps-uat/__fixtures__/plan/state-idea.json"
      provides: "Shift state.json fixture at phase=idea (fresh project)"
      contains: "\"phase\": \"idea\""
    - path: "dashboard/.planning/phases/10-plan-mode-projects-prds-roadmaps-uat/__fixtures__/plan/state-prd-drafting.json"
      provides: "Shift state.json fixture at phase=prd, user_approved=false"
      contains: "\"phase\": \"prd\""
    - path: "dashboard/.planning/phases/10-plan-mode-projects-prds-roadmaps-uat/__fixtures__/plan/state-roadmap-ready.json"
      provides: "Shift state.json fixture at phase=roadmap, prd.user_approved=true, roadmap.user_approved=false"
      contains: "\"user_approved\": true"
    - path: "dashboard/.planning/phases/10-plan-mode-projects-prds-roadmaps-uat/__fixtures__/plan/state-waiting-plans.json"
      provides: "Shift state.json fixture at phase=waiting_for_plans (Shift v3.0 gap state)"
      contains: "waiting_for_plans"
    - path: "dashboard/.planning/phases/10-plan-mode-projects-prds-roadmaps-uat/__fixtures__/plan/ROADMAP.md"
      provides: "Multi-phase ROADMAP fixture with Definition of done bullets for UAT parsing"
      contains: "Definition of done"
    - path: "dashboard/lib/cae-types.ts"
      provides: "Project type extended with shiftPhase + shiftUpdated"
      contains: "shiftPhase"
    - path: "dashboard/lib/cae-shift.test.ts"
      provides: "Failing test scaffolds for resolveProject, readShiftState, buildAnswersFile, approveGate"
      min_lines: 40
    - path: "dashboard/lib/cae-plan-gen.test.ts"
      provides: "Failing test scaffolds for extractPhase1, writeBuildplan, stubPlan"
      min_lines: 30
    - path: "dashboard/lib/cae-uat.test.ts"
      provides: "Failing test scaffolds for parseSuccessCriteria, patchUatState, orphan detection"
      min_lines: 40
    - path: "dashboard/lib/cae-ship.test.ts"
      provides: "Failing test scaffolds for parseEnvExample, validateShipInput, ghAuthStatus"
      min_lines: 30
    - path: "dashboard/.env.example"
      provides: "SHIFT_PROJECTS_HOME documented default"
      contains: "SHIFT_PROJECTS_HOME="
  key_links:
    - from: "dashboard/lib/cae-shift.test.ts"
      to: "dashboard/.planning/phases/10-plan-mode-projects-prds-roadmaps-uat/__fixtures__/plan/state-prd-drafting.json"
      via: "readFileSync(fixtureAbs)"
      pattern: "state-prd-drafting"
    - from: "dashboard/lib/cae-uat.test.ts"
      to: "dashboard/.planning/phases/10-plan-mode-projects-prds-roadmaps-uat/__fixtures__/plan/ROADMAP.md"
      via: "readFileSync(fixtureAbs)"
      pattern: "ROADMAP.md"
    - from: "dashboard/lib/cae-types.ts"
      to: "dashboard/lib/cae-state.ts (consumer)"
      via: "export interface Project"
      pattern: "shiftPhase"
---

<objective>
Wave 0 scaffold for Phase 10. This plan ships **nothing user-visible** — it ships the test harnesses, fixtures, and type contracts that every Wave 1 lib plan will consume. Executing Wave 1 in parallel only works if the types + fixtures are all present first.

**Purpose:** Remove the "go read Shift's source to find the state.json shape" step from every builder's critical path. Fixtures are the contracts.
**Output:** Four failing test suites (by design), four fixture files, one `.env.example` line, one type-file extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@dashboard/.planning/phases/10-plan-mode-projects-prds-roadmaps-uat/10-CONTEXT.md
@dashboard/.planning/phases/10-plan-mode-projects-prds-roadmaps-uat/10-RESEARCH.md
@dashboard/lib/cae-types.ts
@dashboard/lib/cae-state.ts
@dashboard/.env.example

<interfaces>
Shift `state.json` shape (extracted from `/home/shift/bin/shift` v3.0.0 source + RESEARCH):
```json
{
  "schema_version": 1,
  "project_name": "my-project",
  "phase": "idea" | "research" | "prd" | "roadmap" | "waiting_for_plans" | "executing" | "done",
  "updated": "2026-04-23T12:00:00Z",
  "idea": { "what": "...", "who": "...", "type": "web" },
  "prd": { "path": ".shift/PRD.md", "user_approved": false },
  "roadmap": { "path": ".shift/ROADMAP.md", "user_approved": false },
  "history": [
    { "ts": "2026-04-23T12:00:00Z", "action": "idea_captured", "outcome": "ok" }
  ]
}
```
Note: Shift treats absent `prd` / `roadmap` keys as phase-not-yet-reached; tests must cover both undefined and `user_approved: false` cases.

Project type (current, from `dashboard/lib/cae-types.ts` lines 7-11):
```typescript
export interface Project {
  name: string;
  path: string;
  hasPlanning: boolean;
}
```
Extension target (this plan):
```typescript
export interface Project {
  name: string;
  path: string;
  hasPlanning: boolean;
  /** Shift lifecycle phase from `.shift/state.json`. null = not a Shift project. */
  shiftPhase?: string | null;
  /** ISO timestamp from `.shift/state.json::updated`. null = not a Shift project. */
  shiftUpdated?: string | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Shift-shaped fixtures + ROADMAP + .env.example fixture</name>
  <files>
    dashboard/.planning/phases/10-plan-mode-projects-prds-roadmaps-uat/__fixtures__/plan/state-idea.json,
    dashboard/.planning/phases/10-plan-mode-projects-prds-roadmaps-uat/__fixtures__/plan/state-prd-drafting.json,
    dashboard/.planning/phases/10-plan-mode-projects-prds-roadmaps-uat/__fixtures__/plan/state-roadmap-ready.json,
    dashboard/.planning/phases/10-plan-mode-projects-prds-roadmaps-uat/__fixtures__/plan/state-waiting-plans.json,
    dashboard/.planning/phases/10-plan-mode-projects-prds-roadmaps-uat/__fixtures__/plan/PRD.md,
    dashboard/.planning/phases/10-plan-mode-projects-prds-roadmaps-uat/__fixtures__/plan/ROADMAP.md,
    dashboard/.planning/phases/10-plan-mode-projects-prds-roadmaps-uat/__fixtures__/plan/dot-env-example.txt
  </files>
  <action>
Create seven fixture files under `dashboard/.planning/phases/10-plan-mode-projects-prds-roadmaps-uat/__fixtures__/plan/` (new directory).

**`state-idea.json`** — fresh project, only `idea` populated:
```json
{
  "schema_version": 1,
  "project_name": "test-fixture-idea",
  "phase": "idea",
  "updated": "2026-04-23T10:00:00Z",
  "idea": { "what": "founder accountability app", "who": "solo founders", "type": "web" },
  "history": [{ "ts": "2026-04-23T10:00:00Z", "action": "idea_captured", "outcome": "ok" }]
}
```

**`state-prd-drafting.json`** — PRD written but user hasn't approved:
```json
{
  "schema_version": 1,
  "project_name": "test-fixture-prd",
  "phase": "prd",
  "updated": "2026-04-23T10:15:00Z",
  "idea": { "what": "...", "who": "...", "type": "web" },
  "prd": { "path": ".shift/PRD.md", "user_approved": false },
  "history": [
    { "ts": "2026-04-23T10:00:00Z", "action": "idea_captured", "outcome": "ok" },
    { "ts": "2026-04-23T10:15:00Z", "action": "prd_drafted", "outcome": "ok" }
  ]
}
```

**`state-roadmap-ready.json`** — PRD approved, ROADMAP drafted, awaiting approve:
```json
{
  "schema_version": 1,
  "project_name": "test-fixture-roadmap",
  "phase": "roadmap",
  "updated": "2026-04-23T10:30:00Z",
  "idea": { "what": "...", "who": "...", "type": "web" },
  "prd": { "path": ".shift/PRD.md", "user_approved": true },
  "roadmap": { "path": ".shift/ROADMAP.md", "user_approved": false },
  "history": [
    { "ts": "2026-04-23T10:15:00Z", "action": "prd_drafted", "outcome": "ok" },
    { "ts": "2026-04-23T10:20:00Z", "action": "prd_approved", "outcome": "dashboard" },
    { "ts": "2026-04-23T10:30:00Z", "action": "roadmap_drafted", "outcome": "ok" }
  ]
}
```

**`state-waiting-plans.json`** — ROADMAP approved, Shift parked at `waiting_for_plans` (the v3.0 gap D-09 fills):
```json
{
  "schema_version": 1,
  "project_name": "test-fixture-waiting",
  "phase": "waiting_for_plans",
  "updated": "2026-04-23T10:45:00Z",
  "idea": { "what": "...", "who": "...", "type": "web" },
  "prd": { "path": ".shift/PRD.md", "user_approved": true },
  "roadmap": { "path": ".shift/ROADMAP.md", "user_approved": true },
  "history": [
    { "ts": "2026-04-23T10:30:00Z", "action": "roadmap_drafted", "outcome": "ok" },
    { "ts": "2026-04-23T10:40:00Z", "action": "roadmap_approved", "outcome": "dashboard" },
    { "ts": "2026-04-23T10:45:00Z", "action": "waiting_for_plans", "outcome": "v3.0 gap" }
  ]
}
```

**`PRD.md`** — minimal realistic PRD (~40 lines) with `# <Project>` heading, Problem / User / Success criteria / Scope sections.

**`ROADMAP.md`** — 3-phase roadmap. Each `## Phase N: <Title>` block contains a `Definition of done:` bullet list with 2-4 items. This feeds D-10 UAT parsing tests. Example structure:
```
# My Project — Roadmap

## Phase 1: Foundation
Goal: ship the shell.
Definition of done:
- Login works end-to-end
- Home page renders
- Tests pass

## Phase 2: Core feature
Goal: ...
Definition of done:
- Create a thing
- Edit a thing

## Phase 3: Polish
Goal: ...
Definition of done:
- Empty states exist
- Keyboard shortcuts documented
```
Use 6-10 distinct `Definition of done` bullets across the three phases so UAT-id uniqueness can be asserted.

**`dot-env-example.txt`** — sample `.env.example` contents (fixture can't be named `.env.example` verbatim because Next.js refuses to serve some `.env*` paths; the lib test reads by absolute path so any filename works):
```
# Required
DATABASE_URL=postgres://localhost/mydb
NEXTAUTH_SECRET=change-me
NEXTAUTH_URL=http://localhost:3000
# Optional
SENTRY_DSN=

# Comment at end of file
STRIPE_KEY=sk_test_xxx
```
Include 4 valid key=value lines, 2 comment lines, 1 blank line, 1 line with trailing comment (`FOO=bar # inline`) to exercise the regex `/^([A-Z_][A-Z0-9_]*)\s*=\s*(.*)$/mg`.
  </action>
  <verify>
    <automated>test -f dashboard/.planning/phases/10-plan-mode-projects-prds-roadmaps-uat/__fixtures__/plan/state-idea.json && test -f dashboard/.planning/phases/10-plan-mode-projects-prds-roadmaps-uat/__fixtures__/plan/state-prd-drafting.json && test -f dashboard/.planning/phases/10-plan-mode-projects-prds-roadmaps-uat/__fixtures__/plan/state-roadmap-ready.json && test -f dashboard/.planning/phases/10-plan-mode-projects-prds-roadmaps-uat/__fixtures__/plan/state-waiting-plans.json && test -f dashboard/.planning/phases/10-plan-mode-projects-prds-roadmaps-uat/__fixtures__/plan/PRD.md && test -f dashboard/.planning/phases/10-plan-mode-projects-prds-roadmaps-uat/__fixtures__/plan/ROADMAP.md && test -f dashboard/.planning/phases/10-plan-mode-projects-prds-roadmaps-uat/__fixtures__/plan/dot-env-example.txt && node -e "JSON.parse(require('fs').readFileSync('dashboard/.planning/phases/10-plan-mode-projects-prds-roadmaps-uat/__fixtures__/plan/state-prd-drafting.json', 'utf8'))"</automated>
  </verify>
  <done>All seven fixtures exist, all four state.json files parse as valid JSON, ROADMAP.md contains the literal string "Definition of done" at least three times (one per phase).</done>
</task>

<task type="auto">
  <name>Task 2: Extend Project type with shiftPhase + shiftUpdated, update .env.example</name>
  <files>dashboard/lib/cae-types.ts, dashboard/.env.example</files>
  <action>
**`dashboard/lib/cae-types.ts`** — extend the existing `Project` interface (currently lines 7-11). Add two optional fields:
```typescript
export interface Project {
  name: string;
  path: string;
  hasPlanning: boolean;
  /**
   * Phase 10 D-02: Shift lifecycle phase from `.shift/state.json::phase`.
   * null or undefined when the project has no `.shift/` directory (not a Shift project).
   */
  shiftPhase?: string | null;
  /**
   * Phase 10 D-02: ISO timestamp from `.shift/state.json::updated`.
   * null or undefined when the project has no `.shift/` directory.
   * Used as Plan-home sort key (descending).
   */
  shiftUpdated?: string | null;
}
```
Leave the rest of the file unchanged. Do NOT yet modify `lib/cae-state.ts::listProjects` — that lands in plan 10-04. The TS compiler must remain green because both new fields are optional.

**`dashboard/.env.example`** — append (don't replace) a new section at the end:
```
# --- Phase 10: Plan mode (Shift wrapper) ---
# Root directory scanned for Shift-managed projects. Each subdirectory
# containing a `.shift/state.json` file is surfaced on /plan. Default
# matches the host layout on cae. Override per-host if needed.
SHIFT_PROJECTS_HOME=/home/cae
```
If `.env.example` does not exist, create it with this section only.
  </action>
  <verify>
    <automated>cd dashboard && npx tsc --noEmit && grep -q "SHIFT_PROJECTS_HOME" .env.example && grep -q "shiftPhase" lib/cae-types.ts && grep -q "shiftUpdated" lib/cae-types.ts</automated>
  </verify>
  <done>`tsc --noEmit` exits 0, `.env.example` includes `SHIFT_PROJECTS_HOME=/home/cae`, `Project` type exports both `shiftPhase` and `shiftUpdated` as optional string-or-null.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write failing test scaffolds for all six lib modules</name>
  <files>dashboard/lib/cae-shift.test.ts, dashboard/lib/cae-plan-gen.test.ts, dashboard/lib/cae-uat.test.ts, dashboard/lib/cae-ship.test.ts, dashboard/lib/cae-state.test.ts, dashboard/lib/cae-plan-home.test.ts</files>
  <behavior>
Each test file declares Vitest suites that import from a module that does NOT YET EXIST. `npm test` must report 6 failing suites (import errors are failures) — this is the RED step.

**`cae-shift.test.ts`** — at minimum these `it` blocks (all `.skip` is forbidden; they must fail, not skip):
- `describe("resolveProject")`:
  - `it("returns null for unknown slug")`
  - `it("matches by basename when only slug given")`
  - `it("matches by absolute path when full path given")`
  - `it("rejects path traversal attempts like '../etc'")`
- `describe("readShiftState")`:
  - `it("parses state-prd-drafting.json fixture")` — asserts `phase === "prd"`, `prd.user_approved === false`
  - `it("returns null when .shift/state.json missing")`
- `describe("buildAnswersFile")`:
  - `it("writes SHIFT_ANSWERS JSON keyed by idea.what / idea.who / idea.type_ok")`
  - `it("returns absolute path under /tmp")`
- `describe("approveGate")`:
  - `it("patches prd.user_approved=true and advances phase to roadmap")` — uses state-prd-drafting fixture
  - `it("patches roadmap.user_approved=true and advances phase to waiting_for_plans")` — uses state-roadmap-ready fixture
  - `it("appends history entry with outcome=dashboard")`

**`cae-plan-gen.test.ts`**:
- `describe("extractPhase1")`:
  - `it("extracts the '## Phase 1' section verbatim including Definition of done")` — uses ROADMAP.md fixture
  - `it("returns null when no Phase 1 heading exists")`
- `describe("writeBuildplan")`:
  - `it("writes .planning/phases/01-<slug>/BUILDPLAN.md under project root")`
- `describe("stubPlan")`:
  - `it("writes a waiting_for_plans stub PLAN.md when auto-gen fails")`

**`cae-uat.test.ts`**:
- `describe("parseSuccessCriteria")`:
  - `it("returns a Map<phaseNum, UatItem[]> keyed by integer phase")` — uses ROADMAP.md fixture
  - `it("generates 8-char sha1 ids unique per (phase, bullet) pair")`
  - `it("returns empty map for ROADMAP with no Definition of done bullets")`
- `describe("loadUatState / patchUatState")`:
  - `it("creates .planning/uat/phase<N>.json on first patch")`
  - `it("updates status=pass and records ts/note")`
  - `it("flags orphaned=true on items whose id no longer exists in current ROADMAP")`

**`cae-ship.test.ts`**:
- `describe("parseEnvExample")`:
  - `it("extracts whitelisted keys from fixture dot-env-example.txt")` — asserts presence of DATABASE_URL, NEXTAUTH_SECRET, NEXTAUTH_URL, SENTRY_DSN, STRIPE_KEY
  - `it("skips comment lines and blank lines")`
  - `it("handles inline trailing comments")`
- `describe("validateShipInput")`:
  - `it("rejects keys not in the whitelist")`
  - `it("accepts whitelisted keys with empty string values")`
- `describe("ghAuthStatus")`:
  - `it("returns { authed: true } when execFile exits 0")` — mock child_process
  - `it("returns { authed: false } when execFile exits 1")`

**`cae-state.test.ts`** (covers listProjects Shift extension, plan 10-04 target):
- `describe("listProjects with Shift discovery")`:
  - `it("surfaces directories under SHIFT_PROJECTS_HOME with .shift/state.json")`
  - `it("attaches shiftPhase + shiftUpdated from state.json")`
  - `it("sorts by shiftUpdated desc when present")`
  - `it("deduplicates by absolute path against the hard-coded candidates list")`
  - `it("skips directories without .shift/state.json (not a Shift project)")`

**`cae-plan-home.test.ts`** (covers Plan-home aggregator, plan 10-04 target):
- `describe("getPlanHomeState")`:
  - `it("returns { projects, emptyState } shape")`
  - `it("marks emptyState=true when no projects exist")`
  - `it("attaches lifecycleBadge derived from shiftPhase")`
  - `it("returns the mostRecentSlug for default project selection")`
  </behavior>
  <action>
Create six test files under `dashboard/lib/`. Each file:

1. Imports Vitest primitives: `import { describe, it, expect, beforeEach, afterEach, vi } from "vitest"`.
2. Imports the module under test from the sibling path (`./cae-shift`, `./cae-plan-gen`, etc.) — these imports WILL FAIL until Wave 1 lands; that's the point.
3. Imports fixtures via absolute-from-repo-root paths using `path.join(process.cwd(), ".planning/phases/10-plan-mode-projects-prds-roadmaps-uat/__fixtures__/plan/<file>")` — remember Vitest runs with `cwd = dashboard/`, so the path is relative to the dashboard dir, not repo root. Verify by reading Phase 9 test examples (e.g., `dashboard/lib/voice-router.test.ts`, `dashboard/lib/cae-chat-state.test.ts`, `dashboard/lib/chat-suggestions.test.ts`) to match the existing fixture-path convention.
4. Declares the `describe` / `it` blocks in the behavior section above. Each `it` body is a real test — not `it.todo`, not `it.skip`. Use `expect(() => { /* placeholder that fails */ }).not.toThrow()` patterns or direct assertions on imported-but-undefined functions. The goal is a genuine RED state.
5. Uses `vi.mock("child_process")` where the behavior calls out for it (`cae-ship.test.ts` ghAuthStatus).
6. Adds a top-of-file docblock: `// RED-phase scaffold for Phase 10 plan NN. Fails until lib/<module>.ts lands in wave 1.`

**Do NOT create the lib source files** — that's Wave 1's job. If you're tempted to stub them out to "make tests pass," STOP. The whole point of Wave 0 is capturing the contract as executable spec.

Reference Phase 9's `dashboard/lib/voice-router.test.ts` for the idiomatic Vitest + @testing-library shape used in this project. Follow the same import order, same assertion style.
  </action>
  <verify>
    <automated>cd dashboard && npx vitest run lib/cae-shift.test.ts lib/cae-plan-gen.test.ts lib/cae-uat.test.ts lib/cae-ship.test.ts lib/cae-state.test.ts lib/cae-plan-home.test.ts --reporter=verbose 2>&1 | tee /tmp/wave0-verify.log; grep -E "Test Files +[0-9]+ failed" /tmp/wave0-verify.log || grep -E "failed|FAIL" /tmp/wave0-verify.log</automated>
  </verify>
  <done>All six test files exist. `vitest run` reports them as failing (either import errors or assertion failures). No test is `.skip` or `.todo`. Each file contains at least the number of `it(` blocks listed in the behavior section.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (fixtures only) | This plan ships no runtime code paths. Fixtures are consumed by tests in subsequent plans. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | I (Info disclosure) | fixture .env.example | accept | Fixture contains placeholder secrets (`sk_test_xxx`) not real values; named `dot-env-example.txt` to avoid confusion with real `.env` ignore rules |
| T-10-01-02 | T (Tampering) | fixture state.json | accept | Fixtures are read-only by tests; committed to git; tampering = detectable via diff |
</threat_model>

<verification>
- `npx tsc --noEmit` from `dashboard/` exits 0 (Project type extension is backward-compatible).
- `npx vitest run lib/cae-*.test.ts lib/cae-plan-home.test.ts` from `dashboard/` shows 6 failing test files (the intended RED state). Expected failure mode: "Cannot find module './cae-shift'" etc.
- All seven fixture files parse/exist.
- `.env.example` contains `SHIFT_PROJECTS_HOME=/home/cae`.
</verification>

<success_criteria>
- Six test files written with concrete `it` blocks (no `.skip` / `.todo`).
- Seven fixture files written and valid (JSON parses, MD readable).
- `Project` type extended with optional `shiftPhase` + `shiftUpdated`; tsc green.
- `.env.example` documents `SHIFT_PROJECTS_HOME=/home/cae`.
- Every Wave 1 plan (10-02, 10-03, 10-04) can proceed with tests-first without writing its own fixtures.
</success_criteria>

<output>
After completion, create `.planning/phases/10-plan-mode-projects-prds-roadmaps-uat/10-01-SUMMARY.md` listing every fixture path and every test file with their failing test counts.
</output>
