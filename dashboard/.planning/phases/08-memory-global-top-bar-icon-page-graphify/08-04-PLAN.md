---
phase: 08-memory-global-top-bar-icon-page-graphify
plan: 04
type: execute
wave: 3
depends_on: [08-03]
files_modified:
  - dashboard/components/memory/browse/browse-pane.tsx
  - dashboard/components/memory/browse/file-tree.tsx
  - dashboard/components/memory/browse/file-tree.test.tsx
  - dashboard/components/memory/browse/markdown-view.tsx
  - dashboard/components/memory/browse/search-bar.tsx
  - dashboard/components/memory/browse/search-results.tsx
  - dashboard/components/memory/browse/search-results.test.tsx
autonomous: true
requirements:
  - MEM-02
  - MEM-03
  - MEM-04
tags: [wave-3, browse-tab, file-tree, markdown, ripgrep-ui, parallel-with-08-05]

must_haves:
  truths:
    - "`components/memory/browse/browse-pane.tsx` is a `\"use client\"` component that fetches `/api/memory/tree` on mount and composes FileTree + MarkdownView + SearchBar + SearchResults"
    - "`FileTree` renders nested MemoryTreeNodes with expand/collapse; clicking a leaf calls `onSelect(absPath)`"
    - "`MarkdownView` fetches `/api/memory/file/<path>` for the selected path and renders via `react-markdown` + `remark-gfm`; shows loading/error/not-found states"
    - "`SearchBar` is a controlled input with 200-char cap + 300ms debounce; submits to `/api/memory/search`"
    - "`SearchResults` lists hits grouped by file, click → selects that file in FileTree (via callback prop)"
    - "All components live under `components/memory/browse/` — zero overlap with `components/memory/graph/` from plan 08-05"
    - "Zero usage of `asChild` on base-ui components (AGENTS.md `p2-plA-t1-e81f6c`)"
    - "Vitest: FileTree + SearchResults have unit tests covering render, keyboard nav, selection callback"
    - "`pnpm tsc --noEmit`, `pnpm build`, and `pnpm test` all exit 0"
  artifacts:
    - path: "dashboard/components/memory/browse/browse-pane.tsx"
      provides: "Tab-level Browse composition"
      contains: "use client"
    - path: "dashboard/components/memory/browse/file-tree.tsx"
      provides: "Recursive tree UI"
      exports: ["FileTree"]
    - path: "dashboard/components/memory/browse/markdown-view.tsx"
      provides: "Markdown render for selected file"
      contains: "react-markdown"
    - path: "dashboard/components/memory/browse/search-bar.tsx"
      provides: "Debounced search input"
      exports: ["SearchBar"]
    - path: "dashboard/components/memory/browse/search-results.tsx"
      provides: "Grouped rg-hit display"
      exports: ["SearchResults"]
  key_links:
    - from: "dashboard/components/memory/browse/browse-pane.tsx"
      to: "/api/memory/tree"
      via: "fetch on mount"
      pattern: "/api/memory/tree"
    - from: "dashboard/components/memory/browse/markdown-view.tsx"
      to: "/api/memory/file"
      via: "fetch on path change"
      pattern: "/api/memory/file"
    - from: "dashboard/components/memory/browse/search-bar.tsx"
      to: "/api/memory/search"
      via: "debounced POST/GET"
      pattern: "/api/memory/search"
    - from: "dashboard/components/memory/browse/markdown-view.tsx"
      to: "react-markdown"
      via: "import ReactMarkdown"
      pattern: "react-markdown"
---

<objective>
Ship the Browse tab's client island: file tree of all memory sources across projects (via `/api/memory/tree`), markdown viewer for a selected file (via `/api/memory/file/[...path]` + `react-markdown`), and a debounced ripgrep-backed full-text search UI (via `/api/memory/search`). Components live in `components/memory/browse/` in strict isolation from plan 08-05's graph components so the two plans execute in parallel.

Purpose: Browse is the founder's primary entry to memory — "click through what CAE remembers." Must feel fast (tree pre-loaded, markdown on-demand, search debounced).

Output: 5 TSX components (browse-pane, file-tree, markdown-view, search-bar, search-results) + 2 Vitest suites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@dashboard/.planning/phases/08-memory-global-top-bar-icon-page-graphify/08-CONTEXT.md
@dashboard/.planning/phases/08-memory-global-top-bar-icon-page-graphify/08-03-PLAN.md
@dashboard/lib/cae-memory-sources.ts
@dashboard/lib/cae-memory-search.ts
@dashboard/lib/copy/labels.ts
@dashboard/components/ui/explain-tooltip.tsx
@dashboard/lib/providers/explain-mode.tsx
@dashboard/lib/providers/dev-mode.tsx

<interfaces>
<!-- Server module types this tab consumes (frozen in plan 08-03). -->

From lib/cae-memory-sources.ts:
```typescript
interface MemoryTreeNode {
  id: string;
  label: string;
  absPath?: string;         // present on `kind: "file"` leaves
  kind: "project" | "group" | "file";
  children?: MemoryTreeNode[];
}
```

From lib/cae-memory-search.ts:
```typescript
interface SearchHit { file: string; line: number; preview: string; }
```

Route responses:
- `GET /api/memory/tree` → `{ projects: MemoryTreeNode[] }`
- `GET /api/memory/file/<path>` → `{ path: string, contents: string, size: number }` OR 404 OR 403
- `GET /api/memory/search?q=...` → `{ q: string, hits: SearchHit[] }` OR 400

From labels.ts (memory.* keys — Phase 8 Wave 2 added):
- `memoryTabBrowse`, `memorySearchPlaceholder`, `memoryFileNotFound`, `memoryLoadFailed`, `memoryEmptyBrowse`, `memoryExplainSearch`

From components/ui/explain-tooltip.tsx (Wave 0):
- `<ExplainTooltip text={string} ariaLabel={string} />`

From lib/providers/*:
- `useExplainMode(): { explain: boolean }`
- `useDevMode(): { dev: boolean }` → feed into `labelFor(dev)`.

Tailwind tokens in use (per UI-SPEC §13):
- `--bg`, `--surface`, `--border`, `--text`, `--text-muted`, `--accent`, `--accent-muted`.
- `text-[color:var(--text)]`, `bg-[color:var(--surface)]`, etc.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build FileTree + MarkdownView + BrowsePane composition</name>
  <files>dashboard/components/memory/browse/browse-pane.tsx, dashboard/components/memory/browse/file-tree.tsx, dashboard/components/memory/browse/file-tree.test.tsx, dashboard/components/memory/browse/markdown-view.tsx</files>
  <read_first>
    - dashboard/components/ui/explain-tooltip.tsx (import pattern + variant)
    - 08-CONTEXT.md D-09 (react-markdown + remark-gfm pins)
    - dashboard/lib/copy/labels.ts memory.* keys (how to label tree groups, empty states)
  </read_first>
  <action>
    **A) `components/memory/browse/file-tree.tsx`** (client):
    - Props: `{ nodes: MemoryTreeNode[]; selectedPath: string | null; onSelect: (absPath: string) => void; }`.
    - Renders recursive `<ul>/<li>` tree. Branch nodes show expand/collapse chevron (Lucide `ChevronRight`/`ChevronDown`). Leaf nodes are `<button>` with full abs path as title; click → `onSelect(absPath)`.
    - Expand state = local `useState<Set<string>>` keyed by node id; default: project-level + first-level group expanded, deeper levels collapsed.
    - Selected leaf gets `data-selected` + cyan border styling.
    - Keyboard nav: arrow keys move focus; Enter = select; Right = expand; Left = collapse.
    - Empty state: if `nodes.length === 0`, render `{labels.memoryEmptyBrowse}`.
    - Dense typography per UI-SPEC §13 "dense lists" — 12-14px padding, mono for leaf labels.

    **B) `components/memory/browse/markdown-view.tsx`** (client):
    - Props: `{ absPath: string | null }`.
    - On `absPath` change, fetch `/api/memory/file/` + encoded absPath → `setState` content/error/loading.
    - Renders `<ReactMarkdown remarkPlugins={[remarkGfm]}>{contents}</ReactMarkdown>`.
    - Error states: 404 → `{labels.memoryFileNotFound}`, 403 → "Not a memory source" (dev-mode adds "allowlist reject"), other → `{labels.memoryLoadFailed}`.
    - Loading: `<div className="animate-pulse">Loading…</div>`.
    - Empty state (no `absPath`): "Pick a file from the left to read it." (+ explain tooltip).
    - Breathable typography per UI-SPEC §13 "detail": 24-32px padding, sans 14-16px, generous line-height (`prose prose-invert` OR hand-rolled). DO NOT add Tailwind typography plugin — hand-style the essential elements: h1/h2/h3 sizes, code inline/block, lists, blockquotes.
    - Title row shows file path (mono, `text-muted`), Git-timeline button (stub — opens drawer wired in Wave 5), raw-view toggle.

    **C) `components/memory/browse/browse-pane.tsx`** (client):
    - Fetches `/api/memory/tree` once on mount.
    - Local state: `selectedPath: string | null` (seeded from `selectedPath` prop if provided, else `initialPath` prop, else `null`).
    - Layout: two-column flex — `<FileTree>` on left (300px fixed), `<MarkdownView>` on right (flex-1).
    - Handles tree loading + error.
    - **Full prop signature (declared here, honored by Wave 5 without refactor):**
      ```ts
      export interface BrowsePaneProps {
        initialPath?: string;                              // deep-link seed (Wave 5 `?path=<rel>`)
        selectedPath?: string | null;                      // controlled override from parent (Wave 5)
        onSelect?: (absPath: string) => void;              // notify parent when user picks a file (Wave 5 URL sync)
        onOpenGitTimeline?: (absPath: string) => void;     // open Git-timeline drawer for this file (Wave 5)
      }
      ```
      If `selectedPath` prop is provided, BrowsePane runs as a controlled component (local state mirrors prop). If omitted, falls back to uncontrolled (seeded from `initialPath`).
      `onSelect` is called inside the internal file-tree click handler BEFORE local state update, so parent can cancel or sync URL.
      `onOpenGitTimeline` is passed through to `MarkdownView` so its Git-timeline button fires this callback. If omitted, the button is hidden (no-op default).
    - Includes SearchBar + SearchResults at the TOP of the file-tree column (Task 2's components) — for this task, leave TWO import stubs pointing at `./search-bar` + `./search-results` plus a comment `// TODO(08-04 task 2): wire search state` — Task 2 fills them in.

    **D) `components/memory/browse/file-tree.test.tsx` (Vitest + RTL, 4 cases):**
    1. Empty nodes array → renders empty state copy.
    2. Two-level tree (project > group > file) → renders both levels; click leaf → `onSelect(absPath)` called with correct arg.
    3. Clicking chevron collapses/expands a group; leaf disappears when collapsed.
    4. Arrow-down keyboard nav moves focus to next focusable leaf.

    **E) Constraints:**
    - All three files start with `"use client";`.
    - No base-ui `asChild` — use `className` (AGENTS.md `p2-plA-t1-e81f6c`).
    - No import from `components/memory/graph/*` — physical isolation from plan 08-05.
    - All copy through `labels = labelFor(dev)` from `useDevMode` — no hardcoded strings.
    - No `<iframe>`. No `window.open`. Navigation via `onSelect` callback only.
  </action>
  <verify>
    <automated>cd /home/cae/ctrl-alt-elite/dashboard && test -f components/memory/browse/browse-pane.tsx && test -f components/memory/browse/file-tree.tsx && test -f components/memory/browse/markdown-view.tsx && test -f components/memory/browse/file-tree.test.tsx && grep -q '^"use client"' components/memory/browse/browse-pane.tsx && grep -q '^"use client"' components/memory/browse/file-tree.tsx && grep -q '^"use client"' components/memory/browse/markdown-view.tsx && grep -q 'react-markdown' components/memory/browse/markdown-view.tsx && grep -q 'remark-gfm' components/memory/browse/markdown-view.tsx && grep -q 'MemoryTreeNode' components/memory/browse/file-tree.tsx && ! grep -qE 'asChild' components/memory/browse/ -r && pnpm test -- components/memory/browse/file-tree.test.tsx 2>&1 | tee /tmp/test-08-04-t1.log && grep -qE 'passed' /tmp/test-08-04-t1.log && pnpm tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <done>
    Three components live under `components/memory/browse/`. FileTree test suite (4 cases) green. MarkdownView uses `react-markdown` + `remark-gfm`. No `asChild` misuse. `pnpm tsc --noEmit` clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build SearchBar + SearchResults + wire into BrowsePane</name>
  <files>dashboard/components/memory/browse/search-bar.tsx, dashboard/components/memory/browse/search-results.tsx, dashboard/components/memory/browse/search-results.test.tsx, dashboard/components/memory/browse/browse-pane.tsx</files>
  <read_first>
    - Task 1's artifact: browse-pane.tsx (to finish wiring its search section)
    - dashboard/components/ui/explain-tooltip.tsx
    - labels.ts memory.* keys: `memorySearchPlaceholder`, `memoryExplainSearch`, `memoryLoadFailed`
  </read_first>
  <action>
    **A) `components/memory/browse/search-bar.tsx`** (client):
    - Props: `{ onResults: (hits: SearchHit[], q: string) => void; onQueryChange?: (q: string) => void; }`.
    - Controlled `<input>` element. Max length 200 (enforced server-side + here via `maxLength`).
    - 300ms debounce on input; after debounce, fetch `/api/memory/search?q=...`; call `onResults(hits, q)`.
    - Empty query → calls `onResults([], "")` (clears).
    - Loading indicator: small pulsing dot next to input.
    - Explain tooltip using `memoryExplainSearch` copy.
    - Placeholder: `memorySearchPlaceholder`.

    **B) `components/memory/browse/search-results.tsx`** (client):
    - Props: `{ hits: SearchHit[]; q: string; onSelectFile: (absPath: string) => void; }`.
    - If `q === ""` renders null (hidden).
    - If `hits.length === 0` and `q.length > 0`, renders `"No matches for \"{q}\""` muted.
    - Otherwise groups hits by `file`, renders each file header (click → `onSelectFile(file)`) + the first 3 matching lines per file ("+ N more" if truncated). Line preview highlights the matched substring in cyan.
    - Dense layout (mono 12px, 8-10px row padding).

    **C) Finish `browse-pane.tsx` wiring:**
    - Add state: `hits: SearchHit[]`, `q: string`.
    - `<SearchBar onResults={(h, q) => { setHits(h); setQ(q); }} />` at top of left column.
    - `<SearchResults hits={hits} q={q} onSelectFile={(abs) => setSelectedPath(abs)} />` below the search bar, ABOVE the FileTree.
    - Remove the TODO comment from Task 1.

    **D) `components/memory/browse/search-results.test.tsx` (4 cases):**
    1. `q = ""` → renders null.
    2. `q = "foo"`, `hits = []` → renders "No matches for \"foo\"".
    3. Hits across two files → two file headers, clicking first → `onSelectFile` called with that file path.
    4. A single file with 5 hits → only 3 shown + "+2 more" text.

    **E) Constraints:**
    - No third-party debounce lib — use `useEffect` with `setTimeout` + cleanup. 12-line implementation max.
    - Input does not autofocus (the user picks when to search).
    - NO `useRef` to imperatively focus file tree on result click — use state + `onSelect` → tree rerenders with `selectedPath` + `data-selected`.
  </action>
  <verify>
    <automated>cd /home/cae/ctrl-alt-elite/dashboard && test -f components/memory/browse/search-bar.tsx && test -f components/memory/browse/search-results.tsx && test -f components/memory/browse/search-results.test.tsx && grep -q '^"use client"' components/memory/browse/search-bar.tsx && grep -q '^"use client"' components/memory/browse/search-results.tsx && grep -q '/api/memory/search' components/memory/browse/search-bar.tsx && grep -q 'maxLength' components/memory/browse/search-bar.tsx && grep -q 'setTimeout' components/memory/browse/search-bar.tsx && grep -q '<SearchBar' components/memory/browse/browse-pane.tsx && grep -q '<SearchResults' components/memory/browse/browse-pane.tsx && ! grep -qE 'TODO.*task 2' components/memory/browse/browse-pane.tsx && pnpm test -- components/memory/browse/search-results.test.tsx 2>&1 | tee /tmp/test-08-04-t2.log && grep -qE 'passed' /tmp/test-08-04-t2.log && pnpm tsc --noEmit 2>&1 | tail -3 && pnpm build 2>&1 | tail -10 | tee /tmp/build-08-04-t2.log && ! grep -qiE 'Failed to compile' /tmp/build-08-04-t2.log</automated>
  </verify>
  <done>
    SearchBar debounces + hits API correctly; SearchResults groups + truncates as specified; BrowsePane fully wires both into its left column; TODO marker removed; test suite + build + tsc all green.
  </done>
</task>

</tasks>

<verification>
- All 5 TSX files present under `components/memory/browse/`
- Zero `components/memory/graph/*` imports (parallel isolation with plan 08-05)
- Vitest: FileTree (4) + SearchResults (4) all pass
- `pnpm tsc --noEmit` + `pnpm build` green
- No `asChild` on base-ui components
</verification>

<success_criteria>
1. BrowsePane composes SearchBar + SearchResults + FileTree + MarkdownView into a two-column layout.
2. FileTree renders MemoryTreeNodes with expand/collapse + keyboard nav + selection callback.
3. MarkdownView fetches selected file via `/api/memory/file` and renders via `react-markdown` + `remark-gfm` with 404/403/loading states.
4. SearchBar: 300ms debounce, 200-char cap, calls `/api/memory/search`.
5. SearchResults groups by file, truncates at 3 hits/file + "+N more".
6. Zero file overlap with plan 08-05 — both plans live in disjoint subdirs.
7. All Vitest suites pass, TypeScript + build clean.
</success_criteria>

<output>
After completion, create `.planning/phases/08-memory-global-top-bar-icon-page-graphify/08-04-SUMMARY.md`:
- Component file list + line counts
- Vitest output for both suites
- Known visual polish items deferred to Wave 7 UAT (animations, highlight color tuning, mobile considerations)
- Commit hashes per task (2 expected)
</output>
