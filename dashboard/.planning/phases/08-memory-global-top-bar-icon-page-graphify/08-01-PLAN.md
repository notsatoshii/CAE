---
phase: 08-memory-global-top-bar-icon-page-graphify
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - /home/cae/ctrl-alt-elite/.gitignore
  - dashboard/package.json
  - dashboard/pnpm-lock.yaml
  - dashboard/vitest.config.ts
  - dashboard/app/globals.css
  - dashboard/components/ui/explain-tooltip.tsx
  - dashboard/components/metrics/explain-tooltip.tsx
  - dashboard/components/metrics/speed-panel.tsx
  - dashboard/components/metrics/reliability-panel.tsx
  - dashboard/components/metrics/spending-panel.tsx
  - dashboard/.planning/phases/08-memory-global-top-bar-icon-page-graphify/fixtures/graphify-smoke/README.md
  - dashboard/.planning/phases/08-memory-global-top-bar-icon-page-graphify/fixtures/graphify-smoke/graph.sample.json
autonomous: true
requirements:
  - MEM-W0-GRAPHIFY
  - MEM-W0-DEPS
  - MEM-W0-VITEST
  - MEM-W0-EXPLAIN
  - MEM-W0-GITIGNORE
tags: [wave-0, prereqs, install, vitest, explain-tooltip-relocation, graphify, react-flow-css]

must_haves:
  truths:
    - "`graphify --version` on PATH returns a 0.4.x version (install produced `graphifyy` from PyPI)"
    - "`dashboard/package.json` dependencies include `@xyflow/react@12.10.2`, `@dagrejs/dagre@3.0.0`, `react-markdown@10.1.0`, `remark-gfm@4.0.1`; devDependencies include `vitest`, `@vitest/ui`, `jsdom`, `@testing-library/react`, `@testing-library/jest-dom`"
    - "`dashboard/package.json` scripts include `\"test\": \"vitest run\"` and `\"test:watch\": \"vitest\"`"
    - "`dashboard/vitest.config.ts` exists, uses `environment: \"jsdom\"`, and declares the `@` path alias"
    - "`dashboard/app/globals.css` contains exactly ONE `@xyflow/react/dist/style.css` import placed after the `@import \"tailwindcss\"` line"
    - "`dashboard/components/ui/explain-tooltip.tsx` exists and is a byte-faithful move of the old `components/metrics/explain-tooltip.tsx` (same signature, same JSX)"
    - "`dashboard/components/metrics/explain-tooltip.tsx` DOES NOT exist after the move (file deleted)"
    - "The three Phase-7 metrics panels (speed-panel, reliability-panel, spending-panel) import ExplainTooltip from `@/components/ui/explain-tooltip` — zero references to `./explain-tooltip` remain in `components/metrics/**`"
    - "`/home/cae/ctrl-alt-elite/.gitignore` contains a `.cae/` rule (matches both `.cae/` directly at CAE root and nested `.cae/` dirs per standard git behaviour)"
    - "Wave 0 fixture graphify run produced a real `graph.json` under `.planning/phases/08-memory-global-top-bar-icon-page-graphify/fixtures/graphify-smoke/graph.sample.json` whose top-level shape is `{ nodes: [...], edges: [...] }` with at least `id` on each node"
    - "`pnpm tsc --noEmit` passes with zero errors after the ExplainTooltip move"
    - "`pnpm test` exits 0 (the pre-existing .test.ts files from Phase 6 now execute for the first time)"
    - "`pnpm build` succeeds"
  artifacts:
    - path: "dashboard/package.json"
      provides: "Phase 8 deps + test script"
      contains: "@xyflow/react"
    - path: "dashboard/vitest.config.ts"
      provides: "Vitest jsdom config + @ alias"
      contains: "jsdom"
    - path: "dashboard/app/globals.css"
      provides: "React-Flow css import in Tailwind-v4 position"
      contains: "@xyflow/react/dist/style.css"
    - path: "dashboard/components/ui/explain-tooltip.tsx"
      provides: "Shared ExplainTooltip primitive"
      exports: ["ExplainTooltip"]
    - path: "/home/cae/ctrl-alt-elite/.gitignore"
      provides: "Excludes `.cae/` runtime state at CAE root"
      contains: ".cae/"
    - path: "dashboard/.planning/phases/08-memory-global-top-bar-icon-page-graphify/fixtures/graphify-smoke/graph.sample.json"
      provides: "Real graphify output captured once to freeze downstream types"
      contains: "\"nodes\""
  key_links:
    - from: "dashboard/components/metrics/speed-panel.tsx"
      to: "dashboard/components/ui/explain-tooltip.tsx"
      via: "import { ExplainTooltip } from \"@/components/ui/explain-tooltip\""
      pattern: "@/components/ui/explain-tooltip"
    - from: "dashboard/components/metrics/reliability-panel.tsx"
      to: "dashboard/components/ui/explain-tooltip.tsx"
      via: "import { ExplainTooltip } from \"@/components/ui/explain-tooltip\""
      pattern: "@/components/ui/explain-tooltip"
    - from: "dashboard/components/metrics/spending-panel.tsx"
      to: "dashboard/components/ui/explain-tooltip.tsx"
      via: "import { ExplainTooltip } from \"@/components/ui/explain-tooltip\""
      pattern: "@/components/ui/explain-tooltip"
    - from: "dashboard/app/globals.css"
      to: "node_modules/@xyflow/react/dist/style.css"
      via: "@import at end of file (after tailwindcss)"
      pattern: "@xyflow/react/dist/style.css"
---

<objective>
Ship every prerequisite that later Phase-8 waves depend on, in one atomic plan: install graphify (CLI) + four npm deps + Vitest; add the Tailwind-v4-correct react-flow CSS import to `globals.css`; promote `ExplainTooltip` from a metrics-local component to a shared UI primitive under `components/ui/`; add `.cae/` to the CAE-root `.gitignore`; run graphify once against a small fixture dir to capture a real-world `graph.json` that Wave 2 uses to freeze the `GraphNode`/`GraphEdge` types.

Purpose: Zero UI work — pure prereqs. Without this, Wave 2's `/api/memory/graph` route spawns a binary that isn't installed, the graph tab fails to style correctly, tests can't run, and Phase-7 imports hang when we start deleting the old tooltip file.

Output:
- `graphifyy` installed system-wide (`which graphify` resolves).
- `@xyflow/react`, `@dagrejs/dagre`, `react-markdown`, `remark-gfm` added to `dashboard/package.json` at pinned versions.
- Vitest + RTL + jsdom added as devDeps, `"test": "vitest run"` script, `vitest.config.ts` written.
- `@xyflow/react/dist/style.css` imported exactly once in `app/globals.css`.
- `ExplainTooltip` moved to `components/ui/explain-tooltip.tsx`; old file deleted; three Phase-7 import sites updated.
- `.cae/` added to `/home/cae/ctrl-alt-elite/.gitignore`.
- Fixture graph.json captured for later planning reference.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@dashboard/.planning/phases/08-memory-global-top-bar-icon-page-graphify/08-CONTEXT.md
@dashboard/.planning/phases/08-memory-global-top-bar-icon-page-graphify/08-RESEARCH.md
@dashboard/package.json
@dashboard/app/globals.css
@dashboard/components/metrics/explain-tooltip.tsx
@dashboard/components/metrics/speed-panel.tsx
@dashboard/components/metrics/reliability-panel.tsx
@dashboard/components/metrics/spending-panel.tsx
@/home/cae/ctrl-alt-elite/.gitignore

<interfaces>
<!-- Current ExplainTooltip signature (preserved exactly by the move — no behavior change). -->

From dashboard/components/metrics/explain-tooltip.tsx (to be moved verbatim to components/ui/):
```typescript
interface ExplainTooltipProps {
  text: string;
  ariaLabel?: string;
}
export function ExplainTooltip({ text, ariaLabel }: ExplainTooltipProps): JSX.Element;
```

Current importers (all use relative `./explain-tooltip`):
- components/metrics/speed-panel.tsx:28
- components/metrics/reliability-panel.tsx:34
- components/metrics/spending-panel.tsx:38

After the move, all three re-import from `@/components/ui/explain-tooltip` (absolute alias per tsconfig).

Graphify expected output (from 08-RESEARCH.md §Graphify tradeoff, MEDIUM confidence — Wave 0 fixture run CONFIRMS):
```jsonc
{
  "nodes": [
    { "id": "<path-or-entity>", "label": "<display>", "source_file": "<abs or rel>", ... }
  ],
  "edges": [
    { "source": "<node.id>", "target": "<node.id>", "relation": "<kind>", "confidence": "EXTRACTED"|"INFERRED"|"AMBIGUOUS" }
  ]
}
```
Wave 2 freezes the real shape into TypeScript interfaces — but this Wave-0 task just captures the file and confirms it's parseable JSON with `nodes` and `edges` top-level keys.

From dashboard/lib/cae-config.ts:
- `CAE_ROOT` default = `/home/cae/ctrl-alt-elite`. Used later; not needed for this Wave-0 plan.

From dashboard/tsconfig.json (already configured):
- `"paths": { "@/*": ["./*"] }` — the `@/components/ui/*` alias is already valid.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install graphify + npm deps + CAE-root .gitignore patch + React-Flow CSS import</name>
  <files>dashboard/package.json, dashboard/pnpm-lock.yaml, dashboard/app/globals.css, /home/cae/ctrl-alt-elite/.gitignore</files>
  <read_first>
    - dashboard/package.json (FULL file — understand existing `pnpm.overrides` and version pins)
    - dashboard/app/globals.css (FULL file — locate the `@import "tailwindcss"` line that the new import must sit AFTER)
    - /home/cae/ctrl-alt-elite/.gitignore (5 lines — minimal file; append policy)
  </read_first>
  <action>
    **A) Install graphifyy (system-wide).** Per D-02.

    Run:
    ```bash
    pip install --break-system-packages graphifyy==0.4.29
    # Sanity check — the entrypoint is `graphify`, not `graphifyy`:
    command -v graphify
    graphify --version
    ```

    If `pip install` refuses `--break-system-packages` (host-policy-dependent), fall back to `pipx install graphifyy==0.4.29`. Either outcome satisfies the requirement as long as `command -v graphify` resolves.

    Do NOT run `graphify install` (the optional command that writes `~/.claude/skills/graphify/SKILL.md`). That's an operator prereq for OTHER use cases; the dashboard calls graphify directly via `execFile`, bypassing the skill.

    **B) Add npm deps.** Per D-08 + D-09.

    From `dashboard/`:
    ```bash
    pnpm add @xyflow/react@12.10.2 @dagrejs/dagre@3.0.0 react-markdown@10.1.0 remark-gfm@4.0.1
    ```
    Do NOT touch `pnpm.overrides` — `react-is@19.2.4` is locked from Phase 7 and must stay.

    Confirm the resulting `package.json` `dependencies` block has all four pins intact (no `^` prefix wandering in — pnpm usually adds `^`; REMOVE the caret manually so the versions are fully pinned for v1 stability):
    - `"@xyflow/react": "12.10.2"`
    - `"@dagrejs/dagre": "3.0.0"`
    - `"react-markdown": "10.1.0"`
    - `"remark-gfm": "4.0.1"`

    **C) Add the React-Flow CSS import to `app/globals.css`.** Per D-08 + gotcha #2 (Tailwind-v4 + RF-v12 pattern).

    Open `dashboard/app/globals.css`. Find the line `@import "tailwindcss";` (or similar — the Tailwind-v4 entry point). IMMEDIATELY AFTER that line (next line, or after the block of other tailwind plugins if any), insert:

    ```css
    /* Phase 8 Wave 0 (D-08): React-Flow v12 styles. Must sit after @import "tailwindcss"
     * per the Tailwind-v4 compatibility pattern documented at
     * reactflow.dev/whats-new/2025-10-28. Component files MUST NOT re-import this CSS. */
    @import "@xyflow/react/dist/style.css";
    ```

    Do NOT import it inside any component. Do NOT import it twice.

    **D) Add `.cae/` to the CAE-root `.gitignore`.** Per D-01 + gotcha #8.

    Current `/home/cae/ctrl-alt-elite/.gitignore` is 5 lines. Append (preserve trailing newline; add a section comment):

    ```
    # CAE runtime state (metrics jsonl, graph.json — regenerated per run)
    .cae/
    ```

    Place at the END of the file. This is the root-level `.gitignore`; the `dashboard/.gitignore` already excludes `.cae/` separately and is not touched.

    **E) Do NOT delete the existing `fixtures/` directory if it exists.** (Task 3 writes the fixture graph.json. This task just prepares the install.)
  </action>
  <verify>
    <automated>command -v graphify && graphify --version | grep -E '^graphify.* 0\.4\.' && cd /home/cae/ctrl-alt-elite/dashboard && grep -q '"@xyflow/react": "12.10.2"' package.json && grep -q '"@dagrejs/dagre": "3.0.0"' package.json && grep -q '"react-markdown": "10.1.0"' package.json && grep -q '"remark-gfm": "4.0.1"' package.json && grep -q '@xyflow/react/dist/style.css' app/globals.css && [ "$(grep -c '@xyflow/react/dist/style.css' app/globals.css)" -eq 1 ] && awk '/@import "tailwindcss"/{tw=NR} /@xyflow\/react\/dist\/style.css/{rf=NR} END{exit !(tw>0 && rf>tw)}' app/globals.css && grep -qE '^\.cae/' /home/cae/ctrl-alt-elite/.gitignore && pnpm install --frozen-lockfile 2>&1 | tail -5 && pnpm tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    `graphify` resolves on PATH with 0.4.x version; four npm deps pinned exactly in `package.json`; one-and-only-one `@xyflow/react/dist/style.css` import in `app/globals.css` sitting AFTER `@import "tailwindcss"`; `.cae/` added to CAE-root `.gitignore`; `pnpm install --frozen-lockfile` + `pnpm tsc --noEmit` clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Move ExplainTooltip to components/ui/ + update 3 Phase-7 importers</name>
  <files>dashboard/components/ui/explain-tooltip.tsx, dashboard/components/metrics/explain-tooltip.tsx, dashboard/components/metrics/speed-panel.tsx, dashboard/components/metrics/reliability-panel.tsx, dashboard/components/metrics/spending-panel.tsx</files>
  <read_first>
    - dashboard/components/metrics/explain-tooltip.tsx (full file — 60 lines; verbatim copy source)
    - dashboard/components/metrics/speed-panel.tsx lines 25-35 (import + usage context)
    - dashboard/components/metrics/reliability-panel.tsx lines 30-40 (import + usage context)
    - dashboard/components/metrics/spending-panel.tsx lines 35-45 (import + usage context)
  </read_first>
  <action>
    **A) Create `dashboard/components/ui/explain-tooltip.tsx`.**

    BYTE-FAITHFUL copy of the current `dashboard/components/metrics/explain-tooltip.tsx` with ONE surgical edit: update the docstring's phase reference from "Phase 7 — ExplainTooltip (REQ-7-FOUNDER, D-11)." to add a trailing line `"(D-15): relocated to components/ui/ in Phase 8 Wave 0 for cross-phase reuse."`. Everything else — `"use client"`, imports, `ExplainTooltipProps` interface, the `Popover.Root` JSX, className strings, `data-testid` attributes, ariaLabel fallback — is preserved character-for-character. Do NOT refactor, rename, or change styling.

    **B) Delete the old file.** After the new file exists, `rm dashboard/components/metrics/explain-tooltip.tsx`.

    **C) Rewrite the three Phase-7 importers.** Each currently has a single line:
    ```typescript
    import { ExplainTooltip } from "./explain-tooltip";
    ```
    Change to:
    ```typescript
    import { ExplainTooltip } from "@/components/ui/explain-tooltip";
    ```
    Exact files + expected line numbers (subject to file drift — search to confirm):
    - `components/metrics/speed-panel.tsx` line 28
    - `components/metrics/reliability-panel.tsx` line 34
    - `components/metrics/spending-panel.tsx` line 38

    Use `grep -l 'from "./explain-tooltip"' components/metrics/` to double-check the importer set before editing. If any additional file is found, rewrite that too; if grep finds non-Phase-7 consumers outside `components/metrics/`, STOP and re-read the research before proceeding.

    **D) Do NOT add new consumers yet.** Phase 8's Memory components import `@/components/ui/explain-tooltip` in later waves (08-07). This plan only moves the file + rewires EXISTING consumers.

    **E) Guards:**
    - No behavior change.
    - No new exports.
    - No TypeScript errors.
    - `"use client"` remains the first line of the new file.
  </action>
  <verify>
    <automated>cd /home/cae/ctrl-alt-elite/dashboard && test -f components/ui/explain-tooltip.tsx && ! test -e components/metrics/explain-tooltip.tsx && grep -q 'export function ExplainTooltip' components/ui/explain-tooltip.tsx && grep -q '^"use client"' components/ui/explain-tooltip.tsx && grep -q '@/components/ui/explain-tooltip' components/metrics/speed-panel.tsx && grep -q '@/components/ui/explain-tooltip' components/metrics/reliability-panel.tsx && grep -q '@/components/ui/explain-tooltip' components/metrics/spending-panel.tsx && ! grep -rnE 'from "\./explain-tooltip"' components/metrics/ && pnpm tsc --noEmit 2>&1 | tee /tmp/tsc-08-01-t2.log && ! grep -qE 'error TS' /tmp/tsc-08-01-t2.log && pnpm build 2>&1 | tail -10 | tee /tmp/build-08-01-t2.log && ! grep -qiE 'error|failed' /tmp/build-08-01-t2.log</automated>
  </verify>
  <done>
    `components/ui/explain-tooltip.tsx` exists with byte-faithful content + docstring-only edit; old `components/metrics/explain-tooltip.tsx` is gone; three Phase-7 panels import from the new canonical path; zero `./explain-tooltip` relative references remain in `components/metrics/`; `pnpm tsc --noEmit` and `pnpm build` both green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Install Vitest + write vitest.config.ts + run fixture graphify + smoke-test test runner</name>
  <files>dashboard/package.json, dashboard/vitest.config.ts, dashboard/.planning/phases/08-memory-global-top-bar-icon-page-graphify/fixtures/graphify-smoke/README.md, dashboard/.planning/phases/08-memory-global-top-bar-icon-page-graphify/fixtures/graphify-smoke/graph.sample.json</files>
  <read_first>
    - dashboard/package.json (Task 1 already updated deps; here we add devDeps + scripts)
    - dashboard/tsconfig.json (confirm `@` path alias resolves to `./`)
    - dashboard/lib/cae-workflows.test.ts (pre-existing Phase-6 test — must execute under the new runner)
    - dashboard/lib/cae-queue-state.test.ts (pre-existing Phase-6 test)
    - dashboard/lib/cae-nl-draft.test.ts (pre-existing Phase-6 test)
  </read_first>
  <action>
    **A) Install Vitest + RTL + jsdom as devDependencies.** Per D-13.

    From `dashboard/`:
    ```bash
    pnpm add -D vitest@^1.6.0 @vitest/ui@^1.6.0 jsdom@^24.1.0 @testing-library/react@^16.0.0 @testing-library/jest-dom@^6.4.0
    ```
    If pnpm picks newer compatible versions, accept them as long as all five packages land in devDependencies. Exact pins are NOT required for devDeps.

    **B) Add scripts.** In `dashboard/package.json`, extend the `"scripts"` block:
    ```json
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui"
    ```
    Preserve existing `"dev"`, `"build"`, `"start"`, `"lint"` scripts. Do NOT re-order.

    **C) Create `dashboard/vitest.config.ts`.**

    ```typescript
    /// <reference types="vitest" />
    import { defineConfig } from "vitest/config";
    import path from "node:path";

    // Phase 8 Wave 0 (D-13): first-run Vitest config for the dashboard.
    // - jsdom env covers component tests later (Browse / Graph panes).
    // - `@` alias mirrors tsconfig paths so tests and production code import
    //   the same way.
    // - Setup file registers @testing-library/jest-dom custom matchers.
    export default defineConfig({
      test: {
        environment: "jsdom",
        globals: true,
        setupFiles: ["./tests/setup.ts"],
        include: ["lib/**/*.test.ts", "lib/**/*.test.tsx", "components/**/*.test.tsx"],
      },
      resolve: {
        alias: {
          "@": path.resolve(__dirname, "."),
        },
      },
    });
    ```

    Also create `dashboard/tests/setup.ts`:
    ```typescript
    import "@testing-library/jest-dom/vitest";
    ```

    **D) Run graphify against a small fixture.** Per MEM-W0-GRAPHIFY + gotcha #21.

    The fixture dir = `dashboard/.planning/phases/08-memory-global-top-bar-icon-page-graphify/fixtures/graphify-smoke/`. Before running graphify, create a tiny tree of markdown so graphify has input:

    ```bash
    cd dashboard/.planning/phases/08-memory-global-top-bar-icon-page-graphify/fixtures/graphify-smoke
    mkdir -p docs
    cat > docs/a.md <<'EOF'
    # A

    References [B](./b.md).
    EOF
    cat > docs/b.md <<'EOF'
    # B

    Back to [A](./a.md).
    EOF
    cat > README.md <<'EOF'
    # graphify-smoke fixture

    Source tree for the Wave-0 fixture graphify run.
    Two markdown files cross-referencing each other.

    To reproduce:
    ```
    graphify . --mode fast --no-viz --update
    mv graphify-out/graph.json graph.sample.json
    ```
    Captured JSON = `graph.sample.json`. Downstream (Wave 2 `lib/cae-graph-state.ts`) freezes `GraphNode`/`GraphEdge` types against this file.
    EOF
    graphify . --mode fast --no-viz --update
    # Graphify writes graph.json to ./graphify-out/graph.json — move it up one for persistent storage.
    mv graphify-out/graph.json graph.sample.json
    rm -rf graphify-out
    ```

    **HARD REQUIREMENT (upgraded per plan-check Flag 1): the fixture MUST come from a LIVE graphify run. A hand-written fallback is NOT acceptable.**

    Rationale: Wave 2's `lib/cae-graph-state.ts` freezes `GraphNode`/`GraphEdge` types against this file. A guessed schema → the whole downstream graph layer parses wrong shapes. Tradeoffs flagged to fixture — not to code.

    If `graphify . --mode fast --no-viz --update` fails on the 2-file `fixture-input/` (e.g. empty-subdir bug), RETRY against successively larger real fixtures in this order until one succeeds:
    1. `/home/cae/ctrl-alt-elite/docs/` (CAE docs dir — ~10 files)
    2. `/home/cae/ctrl-alt-elite/agents/` (persona md files — ~10 files)
    3. `/home/cae/ctrl-alt-elite/` (whole CAE repo root — --mode fast keeps this deterministic and cheap)

    Record in `README.md` which input dir produced the fixture + the exact command that succeeded + the node/edge count. If ALL three fail, STOP Task 3 and flag a hard blocker — do NOT proceed to Wave 2 with hand-written JSON. Surface the blocker in SUMMARY.md for manual intervention.

    **E) Run the test runner to prove the pre-existing Phase-6 .test.ts files still execute.**

    ```bash
    cd dashboard
    pnpm test
    ```
    Expected: all pre-existing tests pass OR (if Phase-6 tests reveal legit bugs surfaced by first real execution) document those bugs in SUMMARY.md as KNOWN-ISSUE pointers for a later `--gaps` plan — but do NOT fix unrelated test failures in this task. The task succeeds if `pnpm test` exits 0 OR if failures are documented as pre-existing latent bugs; if `pnpm test` cannot even START (config error, missing binary), that IS a failure and must be fixed.

    **F) Do NOT:**
    - Write new tests here. Wave 2 owns first-real-test coverage.
    - Rewrite existing Phase-6 tests (out of scope).
    - Delete `graphify-out/` accidentally if that's where the fixture run put auxiliary files — move only `graph.json`.
    - Run graphify against the real CAE root (huge input, Claude-key dependent if not `--mode fast`). We deliberately run on a 2-file fixture.
  </action>
  <verify>
    <automated>cd /home/cae/ctrl-alt-elite/dashboard && grep -q '"test": "vitest run"' package.json && grep -q '"vitest"' package.json && grep -q '"jsdom"' package.json && test -f vitest.config.ts && grep -q 'environment: "jsdom"' vitest.config.ts && grep -q '"@": path.resolve' vitest.config.ts && test -f tests/setup.ts && grep -q '@testing-library/jest-dom/vitest' tests/setup.ts && test -f .planning/phases/08-memory-global-top-bar-icon-page-graphify/fixtures/graphify-smoke/graph.sample.json && node -e 'const g = require("./.planning/phases/08-memory-global-top-bar-icon-page-graphify/fixtures/graphify-smoke/graph.sample.json"); if (!Array.isArray(g.nodes) || !Array.isArray(g.edges)) { process.exit(1); } if (g.nodes.length < 1) process.exit(1); console.log("fixture ok: " + g.nodes.length + " nodes, " + g.edges.length + " edges");' && pnpm test 2>&1 | tee /tmp/test-08-01-t3.log && grep -qE 'Test Files|passed' /tmp/test-08-01-t3.log</automated>
  </verify>
  <done>
    `pnpm test` runs (Vitest installed + config loaded + jsdom env active + pre-existing Phase-6 tests executed); `vitest.config.ts` and `tests/setup.ts` exist; `fixtures/graphify-smoke/graph.sample.json` is a valid JSON document with `{ nodes: [], edges: [] }` top-level shape and at least one node; fixture `README.md` documents whether the run was live or fallback.
  </done>
</task>

</tasks>

<verification>
- `graphify --version` resolves and returns a 0.4.x version
- `pnpm install --frozen-lockfile` exits 0 after all deps added
- `pnpm tsc --noEmit` exits 0 post-ExplainTooltip-move
- `pnpm build` exits 0
- `pnpm test` exits 0 (or documents pre-existing latent failures without adding new ones)
- `grep -c '@xyflow/react/dist/style.css' dashboard/app/globals.css` returns 1
- `awk` check confirms the RF css import comes AFTER `@import "tailwindcss"` in globals.css
- `grep -c '^\.cae/' /home/cae/ctrl-alt-elite/.gitignore` returns ≥1
- Zero `./explain-tooltip` relative imports remain in `components/metrics/`
- Fixture `graph.sample.json` parses as JSON with `nodes` array ≥1 entry and `edges` array
</verification>

<success_criteria>
1. `graphify` CLI on PATH, version 0.4.x (research-pinned 0.4.29 preferred but any 0.4.x acceptable).
2. Four new npm deps (`@xyflow/react@12.10.2`, `@dagrejs/dagre@3.0.0`, `react-markdown@10.1.0`, `remark-gfm@4.0.1`) installed at exact pins.
3. Vitest + RTL + jsdom installed as devDeps; `"test": "vitest run"` script wired; `vitest.config.ts` + `tests/setup.ts` present; `pnpm test` runs end-to-end.
4. `@xyflow/react/dist/style.css` imported EXACTLY ONCE in `app/globals.css`, positioned after the Tailwind-v4 entry import.
5. `ExplainTooltip` now lives at `components/ui/explain-tooltip.tsx`; old `components/metrics/explain-tooltip.tsx` is deleted; three Phase-7 consumers rewired; zero dangling relative imports.
6. `.cae/` rule present in `/home/cae/ctrl-alt-elite/.gitignore` (CAE root), alongside the pre-existing dashboard-local rule.
7. Fixture `graph.sample.json` captured under `.planning/phases/08-memory-global-top-bar-icon-page-graphify/fixtures/graphify-smoke/` with valid `{ nodes, edges }` shape — this file is the ground-truth schema reference for Wave 2.
8. `pnpm tsc --noEmit` + `pnpm build` + `pnpm test` all exit 0.
</success_criteria>

<output>
After completion, create `.planning/phases/08-memory-global-top-bar-icon-page-graphify/08-01-SUMMARY.md`:
- Exact versions installed for graphify + four npm deps (in case pnpm resolved slightly differently).
- Location of the fixture file + whether the graphify run was LIVE or FALLBACK (and if fallback, why).
- Confirmation that the three Phase-7 panel imports were updated atomically in the same commit as the file move.
- Any deviation from D-08 / D-09 / D-13 / D-15 with justification (there should be none).
- Commit hashes per task (3 expected).
- Any pre-existing Phase-6 test failures newly visible under Vitest (logged but not fixed in this plan — follow-up ticket reference).
</output>
